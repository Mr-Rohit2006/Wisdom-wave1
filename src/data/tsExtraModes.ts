import type { Topic } from "./arcadeQuestions";

// ══════════════════════════════════════════════════
// TYPESCRIPT — OUTPUT GUESS (💻)
// ══════════════════════════════════════════════════
export const TS_OUTPUT_TOPICS: Topic[] = [
  { id:"ts-out-types", name:"Output: Types", icon:"💻", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nlet x: number = 5;\nconsole.log(typeof x);",options:["number","'number'","Number","int"],answer:"number",explanation:"typeof returns a string. typeof 5 = 'number'. TypeScript types are erased at runtime — JS behavior."},
    {id:2,question:"What is the output?\n\nlet x: any = 'hello';\nx = 42;\nconsole.log(typeof x);",options:["string","any","number","Error"],answer:"number",explanation:"any type allows reassignment to any type. After x=42, x is now a number at runtime."},
    {id:3,question:"What is the output?\n\nconst arr: number[] = [1, 2, 3];\nconsole.log(arr.length);",options:["0","2","3","Error"],answer:"3",explanation:"Array has 3 elements. TypeScript number[] is just JavaScript array at runtime — .length = 3."},
    {id:4,question:"What is the output?\n\ntype ID = string | number;\nlet id: ID = '123';\nid = 456;\nconsole.log(typeof id);",options:["string","string | number","number","Error"],answer:"number",explanation:"Union type allows string or number. After id=456, typeof id = 'number' at runtime."},
    {id:5,question:"What is the output?\n\nenum Direction { Up, Down, Left, Right }\nconsole.log(Direction.Up);",options:["'Up'","Up","0","undefined"],answer:"0",explanation:"Numeric enum default values start at 0. Direction.Up = 0, Down = 1, Left = 2, Right = 3."},
    {id:6,question:"What is the output?\n\nenum Status { Active = 'ACTIVE', Inactive = 'INACTIVE' }\nconsole.log(Status.Active);",options:["0","Active","ACTIVE","undefined"],answer:"ACTIVE",explanation:"String enum values are the assigned strings. Status.Active = 'ACTIVE'."},
    {id:7,question:"What is the output?\n\nconst tuple: [string, number] = ['Ali', 25];\nconsole.log(tuple[1]);",options:["'Ali'","Ali","25","Error"],answer:"25",explanation:"Tuple index 1 is the number. tuple[1] = 25. TypeScript tuples are arrays at runtime."},
    {id:8,question:"What is the output?\n\ninterface User { name: string; age?: number; }\nconst u: User = { name: 'Ali' };\nconsole.log(u.age);",options:["null","0","undefined","Error"],answer:"undefined",explanation:"Optional property age not provided — accessing it returns undefined. No runtime error with optional props."},
    {id:9,question:"What is the output?\n\nconst obj = { x: 1, y: 2 } as const;\nconsole.log(obj.x);",options:["readonly 1","1","Error","undefined"],answer:"1",explanation:"as const makes literal types. At runtime it's still a plain object. obj.x = 1."},
    {id:10,question:"What is the output?\n\ntype Result = { ok: true; value: number } | { ok: false; error: string };\nconst r: Result = { ok: true, value: 42 };\nconsole.log(r.ok, (r as any).value);",options:["true 42","true undefined","false 42","Error"],answer:"true 42",explanation:"At runtime plain JS object. r.ok = true, r.value = 42. TypeScript types erased — runtime is pure JS."},
  ]},
  { id:"ts-out-generics", name:"Output: Generics", icon:"💻", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nfunction identity<T>(x: T): T { return x; }\nconsole.log(identity<string>('hello'));",options:["T","string","hello","Error"],answer:"hello",explanation:"Generic identity returns same value passed. identity<string>('hello') returns 'hello'."},
    {id:2,question:"What is the output?\n\nfunction first<T>(arr: T[]): T { return arr[0]; }\nconsole.log(first([10, 20, 30]));",options:["[10,20,30]","10","T","Error"],answer:"10",explanation:"first() returns arr[0]. TypeScript infers T=number. first([10,20,30]) = 10."},
    {id:3,question:"What is the output?\n\nfunction merge<T, U>(a: T, b: U): T & U {\n  return { ...a, ...b } as T & U;\n}\nconst r = merge({ x: 1 }, { y: 2 });\nconsole.log(r.x, r.y);",options:["undefined undefined","1 undefined","1 2","Error"],answer:"1 2",explanation:"merge spreads both objects. Intersection type T & U has all properties. r.x=1, r.y=2."},
    {id:4,question:"What is the output?\n\nclass Box<T> {\n  constructor(public value: T) {}\n}\nconst b = new Box(42);\nconsole.log(b.value);",options:["T","Box","42","Error"],answer:"42",explanation:"Generic class Box stores value. TypeScript infers T=number. b.value = 42."},
    {id:5,question:"What is the output?\n\nfunction getLength<T extends { length: number }>(x: T): number {\n  return x.length;\n}\nconsole.log(getLength('hello'));",options:["T","undefined","5","Error"],answer:"5",explanation:"Constrained generic — T must have length. String has length. 'hello'.length = 5."},
    {id:6,question:"What is the output?\n\ntype Partial<T> = { [P in keyof T]?: T[P] };\ntype User = { name: string; age: number };\nconst u: Partial<User> = {};\nconsole.log(u.name);",options:["''","null","undefined","Error"],answer:"undefined",explanation:"Partial makes all properties optional. u.name not set = undefined. No error — it's optional."},
    {id:7,question:"What is the output?\n\ntype RetType = ReturnType<() => string>;\nconst s: RetType = 'hello';\nconsole.log(s.toUpperCase());",options:["Error","hello","HELLO","RetType"],answer:"HELLO",explanation:"ReturnType<()=>string> = string. s is string. s.toUpperCase() = 'HELLO'."},
    {id:8,question:"What is the output?\n\nconst arr = [1, 'two', true];\nconsole.log(arr.length);",options:["Error","1","3","undefined"],answer:"3",explanation:"TypeScript infers (number | string | boolean)[]. At runtime plain JS array. Length = 3."},
    {id:9,question:"What is the output?\n\nfunction pick<T, K extends keyof T>(obj: T, key: K): T[K] {\n  return obj[key];\n}\nconsole.log(pick({ name: 'Ali', age: 25 }, 'name'));",options:["25","Ali","undefined","Error"],answer:"Ali",explanation:"pick returns obj[key]. key='name', obj.name='Ali'. Return type T[K] = string for 'name' key."},
    {id:10,question:"What is the output?\n\ntype NonNull<T> = T extends null | undefined ? never : T;\ntype Result = NonNull<string | null>;\nconst s: Result = 'hi';\nconsole.log(s);",options:["null","never","hi","Error"],answer:"hi",explanation:"NonNull<string|null> distributes: string (kept) | null→never = string. s is string = 'hi'."},
  ]},
  { id:"ts-out-classes", name:"Output: Classes", icon:"💻", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nclass Animal {\n  constructor(public name: string) {}\n}\nconst a = new Animal('Cat');\nconsole.log(a.name);",options:["undefined","name","Cat","Error"],answer:"Cat",explanation:"constructor shorthand public name: string auto-creates and assigns this.name = 'Cat'."},
    {id:2,question:"What is the output?\n\nclass Counter {\n  static count = 0;\n  constructor() { Counter.count++; }\n}\nnew Counter(); new Counter();\nconsole.log(Counter.count);",options:["0","1","2","Error"],answer:"2",explanation:"Static property shared across instances. Each new Counter() increments. 2 instances = count 2."},
    {id:3,question:"What is the output?\n\nclass Base {\n  greet() { return 'Hello from Base'; }\n}\nclass Child extends Base {\n  greet() { return 'Hello from Child'; }\n}\nconst c: Base = new Child();\nconsole.log(c.greet());",options:["Hello from Base","Hello from Child","Error","undefined"],answer:"Hello from Child",explanation:"Runtime polymorphism — actual type is Child. c.greet() calls Child's override even though type is Base."},
    {id:4,question:"What is the output?\n\nclass MyClass {\n  #secret = 42;\n  getSecret() { return this.#secret; }\n}\nconst obj = new MyClass();\nconsole.log(obj.getSecret());",options:["undefined","Error","#secret","42"],answer:"42",explanation:"#secret is truly private field. getSecret() can access it. Returns 42."},
    {id:5,question:"What is the output?\n\nclass Shape {\n  get area(): number { return 0; }\n}\nclass Circle extends Shape {\n  constructor(private r: number) { super(); }\n  get area(): number { return Math.PI * this.r * this.r; }\n}\nconst c = new Circle(1);\nconsole.log(c.area.toFixed(2));",options:["0","3.14","3.14159","Error"],answer:"3.14",explanation:"Circle overrides area getter. Math.PI * 1 * 1 ≈ 3.14159. toFixed(2) = '3.14'."},
    {id:6,question:"What is the output?\n\nclass Stack<T> {\n  private items: T[] = [];\n  push(item: T) { this.items.push(item); }\n  pop(): T | undefined { return this.items.pop(); }\n}\nconst s = new Stack<number>();\ns.push(1); s.push(2);\nconsole.log(s.pop());",options:["1","2","undefined","Error"],answer:"2",explanation:"Stack is LIFO. push(1) then push(2). pop() returns last = 2."},
    {id:7,question:"What is the output?\n\nabstract class Animal {\n  abstract speak(): string;\n  describe() { return `I say ${this.speak()}`; }\n}\nclass Dog extends Animal {\n  speak() { return 'Woof'; }\n}\nconsole.log(new Dog().describe());",options:["I say abstract","I say Woof","Woof","Error"],answer:"I say Woof",explanation:"describe() calls this.speak() — polymorphic dispatch to Dog.speak() = 'Woof'. Returns 'I say Woof'."},
    {id:8,question:"What is the output?\n\nclass Person {\n  constructor(\n    private name: string,\n    protected age: number\n  ) {}\n}\nclass Employee extends Person {\n  getAge() { return this.age; }\n}\nconst e = new Employee('Ali', 25);\nconsole.log(e.getAge());",options:["Error","undefined","Ali","25"],answer:"25",explanation:"protected age accessible in subclass. Employee.getAge() returns this.age = 25."},
    {id:9,question:"What is the output?\n\nclass MyClass {\n  value = 10;\n  getValue = () => this.value;\n}\nconst obj = new MyClass();\nconst fn = obj.getValue;\nconsole.log(fn());",options:["undefined","Error","10","null"],answer:"10",explanation:"Arrow function captures this lexically in class field. Even when detached, this.value = 10."},
    {id:10,question:"What is the output?\n\nclass A {\n  x = 1;\n}\nclass B extends A {\n  x = 2;\n}\nconst b = new B();\nconsole.log(b.x, (b as A).x);",options:["1 1","2 1","2 2","1 2"],answer:"2 2",explanation:"Both refer to same object b. TypeScript cast (b as A) doesn't change runtime value. b.x = 2."},
  ]},
  { id:"ts-out-advanced", name:"Output: Advanced Types", icon:"💻", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"What is the output?\n\ntype IsString<T> = T extends string ? 'yes' : 'no';\ntype R1 = IsString<string>;\ntype R2 = IsString<number>;\nconst a: R1 = 'yes';\nconst b: R2 = 'no';\nconsole.log(a, b);",options:["yes no","yes yes","no no","Error"],answer:"yes no",explanation:"Conditional type: IsString<string>='yes', IsString<number>='no'. Runtime values match type constraints."},
    {id:2,question:"What is the output?\n\nfunction assertNever(x: never): never {\n  throw new Error('Unexpected: ' + x);\n}\ntype Dir = 'left' | 'right';\nfunction move(d: Dir) {\n  if(d === 'left') return 'going left';\n  if(d === 'right') return 'going right';\n  return assertNever(d);\n}\nconsole.log(move('left'));",options:["going left","going right","Error","never"],answer:"going left",explanation:"move('left') matches first branch returns 'going left'. assertNever never called. Exhaustive check."},
    {id:3,question:"What is the output?\n\nconst obj = { a: 1, b: 'two', c: true };\ntype Keys = keyof typeof obj;\nconst k: Keys = 'a';\nconsole.log(obj[k]);",options:["'a'","Keys","1","Error"],answer:"1",explanation:"keyof typeof obj = 'a' | 'b' | 'c'. k='a' is valid. obj[k] = obj['a'] = 1."},
    {id:4,question:"What is the output?\n\ntype Readonly<T> = { readonly [P in keyof T]: T[P] };\ntype User = { name: string };\nconst u: Readonly<User> = { name: 'Ali' };\nconsole.log(u.name);",options:["Error","readonly","undefined","Ali"],answer:"Ali",explanation:"Readonly mapped type makes all props readonly. At runtime plain object. u.name = 'Ali'."},
    {id:5,question:"What is the output?\n\ntype Awaited<T> = T extends Promise<infer U> ? U : T;\ntype R = Awaited<Promise<string>>;\nconst s: R = 'hello';\nconsole.log(s.length);",options:["Error","5","Promise","hello"],answer:"5",explanation:"Awaited<Promise<string>> = string. s = 'hello'. 'hello'.length = 5."},
    {id:6,question:"What is the output?\n\nconst sym = Symbol('key');\nconst obj: Record<symbol, string> = {};\nobj[sym] = 'value';\nconsole.log(obj[sym]);",options:["key","undefined","Error","value"],answer:"value",explanation:"Symbol as object key. sym is unique symbol. obj[sym] = 'value', obj[sym] retrieves 'value'."},
    {id:7,question:"What is the output?\n\ntype DeepReadonly<T> = {\n  readonly [P in keyof T]: T[P] extends object ? DeepReadonly<T[P]> : T[P]\n};\nconst cfg = { db: { host: 'localhost' } } as DeepReadonly<{db:{host:string}}>;\nconsole.log(cfg.db.host);",options:["Error","readonly","undefined","localhost"],answer:"localhost",explanation:"DeepReadonly is compile-time only. Runtime plain object. cfg.db.host = 'localhost'."},
    {id:8,question:"What is the output?\n\nfunction tag(strings: TemplateStringsArray, ...values: number[]) {\n  return strings[0] + values[0] * 2;\n}\nconst x = 5;\nconsole.log(tag`Value: ${x}`);",options:["Value: 5","Value: 10","Value: x","Error"],answer:"Value: 10",explanation:"Tagged template: strings[0]='Value: ', values[0]=5. Returns 'Value: ' + 5*2 = 'Value: 10'."},
    {id:9,question:"What is the output?\n\nconst obj = {\n  name: 'Ali',\n  greet(this: { name: string }) {\n    return `Hello ${this.name}`;\n  }\n};\nconsole.log(obj.greet());",options:["Hello undefined","Hello Ali","Error","Hello this.name"],answer:"Hello Ali",explanation:"this parameter is type-only. Runtime this = obj. obj.name = 'Ali'. Returns 'Hello Ali'."},
    {id:10,question:"What is the output?\n\ntype Str = string & { __brand: 'Email' };\nfunction makeEmail(s: string): Str {\n  return s as Str;\n}\nconst email = makeEmail('a@b.com');\nconsole.log(email);",options:["Error","{ __brand: 'Email' }","a@b.com","string"],answer:"a@b.com",explanation:"Branded types are compile-time only. Runtime value is just the string 'a@b.com'. No extra property exists."},
  ]},
  { id:"ts-out-async", name:"Output: Async & Modules", icon:"💻", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nasync function greet(): Promise<string> {\n  return 'Hello';\n}\ngreet().then(v => console.log(v));",options:["Promise","Hello","undefined","Error"],answer:"Hello",explanation:"async function returns Promise resolved with 'Hello'. .then receives resolved value = 'Hello'."},
    {id:2,question:"What is the output?\n\nasync function getNum(): Promise<number> {\n  return 42;\n}\nasync function main() {\n  const n = await getNum();\n  console.log(n + 1);\n}\nmain();",options:["42","43","Promise","Error"],answer:"43",explanation:"await unwraps Promise<number> to number 42. 42 + 1 = 43."},
    {id:3,question:"What is the output?\n\nconst p = new Promise<number>((resolve) => {\n  resolve(10);\n});\np.then(v => console.log(v * 2));",options:["10","20","Promise","Error"],answer:"20",explanation:"Promise resolves with 10. .then receives 10. 10 * 2 = 20."},
    {id:4,question:"What is the output?\n\nasync function fail() {\n  throw new Error('oops');\n}\nfail().catch(e => console.log(e.message));",options:["Error","oops","undefined","fail"],answer:"oops",explanation:"async function throwing = rejected Promise. .catch receives Error. e.message = 'oops'."},
    {id:5,question:"What is the output?\n\nconst arr: number[] = [1, 2, 3];\nconst doubled = arr.map(async x => x * 2);\nconsole.log(doubled[0] instanceof Promise);",options:["false","true","undefined","Error"],answer:"true",explanation:"async function always returns Promise. arr.map(async...) creates array of Promises. doubled[0] is Promise."},
    {id:6,question:"What is the output?\n\nfunction delay(ms: number): Promise<void> {\n  return new Promise(resolve => setTimeout(resolve, ms));\n}\nasync function run() {\n  console.log('start');\n  await delay(0);\n  console.log('end');\n}\nrun();\nconsole.log('sync');",options:["start end sync","start sync end","sync start end","Error"],answer:"start sync end",explanation:"run() logs 'start', await yields. 'sync' executes. Then 'end' after microtask/macrotask."},
    {id:7,question:"What is the output?\n\nconst results = await Promise.all([\n  Promise.resolve(1),\n  Promise.resolve(2),\n  Promise.resolve(3)\n]);\nconsole.log(results);",options:["[Promise,Promise,Promise]","[1,2,3]","1,2,3","Error"],answer:"[1,2,3]",explanation:"Promise.all waits for all promises. Returns array of resolved values [1,2,3]."},
    {id:8,question:"What is the output?\n\ntype AsyncFn = () => Promise<number>;\nconst fn: AsyncFn = async () => 42;\nfn().then(v => console.log(typeof v));",options:["Promise","object","number","string"],answer:"number",explanation:"AsyncFn returns Promise<number>. await/then resolves to number 42. typeof 42 = 'number'."},
    {id:9,question:"What is the output?\n\nasync function first() {\n  return await Promise.resolve('hello');\n}\nasync function second() {\n  const r = await first();\n  return r.toUpperCase();\n}\nsecond().then(console.log);",options:["hello","HELLO","Promise","Error"],answer:"HELLO",explanation:"first() resolves 'hello'. second() awaits, calls toUpperCase() = 'HELLO'. .then logs it."},
    {id:10,question:"What is the output?\n\nconst obj = {\n  async getData() {\n    return [1, 2, 3];\n  }\n};\nobj.getData().then(arr => console.log(arr.length));",options:["Promise","3","[1,2,3]","Error"],answer:"3",explanation:"async method returns Promise<number[]>. .then receives [1,2,3]. .length = 3."},
  ]},
];

// ══════════════════════════════════════════════════
// TYPESCRIPT — BUG FINDER (🐛)
// ══════════════════════════════════════════════════
export const TS_BUG_TOPICS: Topic[] = [
  { id:"ts-bug-types", name:"Bug: Types", icon:"🐛", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\nlet age: number = '25';\nconsole.log(age);",options:["No bug","Type error — cannot assign string to number","Missing semicolon","Wrong variable"],answer:"Type error — cannot assign string to number",explanation:"TypeScript enforces type. '25' is string but age is declared as number — Type 'string' is not assignable to type 'number'."},
    {id:2,question:"Find the bug:\n\ninterface User {\n  name: string;\n  age: number;\n}\nconst u: User = { name: 'Ali' };",options:["No bug","Property 'age' is missing — required in User interface","Extra property","Wrong interface"],answer:"Property 'age' is missing — required in User interface",explanation:"age is required (no ?). Omitting it gives: Property 'age' is missing in type '{ name: string; }' but required in type 'User'."},
    {id:3,question:"Find the bug:\n\nconst obj: { name: string } = { name: 'Ali', age: 25 };",options:["No bug","Excess property check — 'age' not in type","Type mismatch","Wrong syntax"],answer:"Excess property check — 'age' not in type",explanation:"Object literals have excess property checking. age is not in { name: string } — Error: 'age' does not exist in type."},
    {id:4,question:"Find the bug:\n\nfunction greet(name: string): string {\n  console.log(`Hello ${name}`);\n}",options:["No bug","Missing return — function declared to return string but returns void","Wrong template","Wrong type"],answer:"Missing return — function declared to return string but returns void",explanation:"Return type is string but function only logs and returns undefined. Must return a string value."},
    {id:5,question:"Find the bug:\n\nconst arr: number[] = [1, 2, 3];\narr.push('four');",options:["No bug","Type error — 'string' not assignable to 'number'","Wrong method","Array error"],answer:"Type error — 'string' not assignable to 'number'",explanation:"arr is number[]. push('four') tries to add string — Argument of type 'string' is not assignable to 'number'."},
    {id:6,question:"Find the bug:\n\ntype Direction = 'left' | 'right' | 'up' | 'down';\nconst d: Direction = 'forward';",options:["No bug","'forward' not in union type Direction","Wrong type alias","Missing quotes"],answer:"'forward' not in union type Direction",explanation:"Literal union type — only 'left' | 'right' | 'up' | 'down' valid. 'forward' is not assignable to type 'Direction'."},
    {id:7,question:"Find the bug:\n\nconst user = { name: 'Ali', age: 25 };\nuser.email = 'ali@mail.com';",options:["No bug","Property 'email' does not exist on inferred type","Wrong assignment","Object error"],answer:"Property 'email' does not exist on inferred type",explanation:"TypeScript infers { name: string; age: number }. email not in inferred type — Property 'email' does not exist."},
    {id:8,question:"Find the bug:\n\nfunction double(x: number) { return x * 2; }\nconsole.log(double('5'));",options:["No bug","Argument of type 'string' not assignable to 'number'","Wrong return","Missing type"],answer:"Argument of type 'string' not assignable to 'number'",explanation:"double expects number. '5' is string — Argument of type 'string' is not assignable to parameter of type 'number'."},
    {id:9,question:"Find the bug:\n\nlet x: string | number = 'hello';\nconsole.log(x.toFixed(2));",options:["No bug","toFixed doesn't exist on 'string | number' — narrow first","Wrong method","Runtime error"],answer:"toFixed doesn't exist on 'string | number' — narrow first",explanation:"toFixed is number method. On union string|number, TypeScript errors — must narrow: if(typeof x === 'number') x.toFixed(2)."},
    {id:10,question:"Find the bug:\n\nconst tuple: [number, string] = [1, 'hello'];\ntuple.push(true);",options:["No bug","Cannot push 'boolean' onto [number, string] tuple","Wrong tuple","Type error"],answer:"Cannot push 'boolean' onto [number, string] tuple",explanation:"Tuple type constrains elements. push(true) violates tuple type — boolean not in (number | string)."},
  ]},
  { id:"ts-bug-interfaces", name:"Bug: Interfaces & Generics", icon:"🐛", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\ninterface Animal {\n  name: string;\n  speak(): void;\n}\nclass Dog implements Animal {\n  name = 'Rex';\n}",options:["No bug","Missing speak() implementation — required by interface","Wrong class","Extra property"],answer:"Missing speak() implementation — required by interface",explanation:"Class Dog must implement all Animal interface members. speak() is missing — Class 'Dog' incorrectly implements interface 'Animal'."},
    {id:2,question:"Find the bug:\n\nfunction getFirst<T>(arr: T[]): T {\n  return arr[0];\n}\nconst result: number = getFirst(['a', 'b', 'c']);",options:["No bug","Type mismatch — string[] infers T=string, but result is number","Generic error","Array error"],answer:"Type mismatch — string[] infers T=string, but result is number",explanation:"getFirst(['a','b','c']) infers T=string, returns string. Assigning to number — Type 'string' not assignable to 'number'."},
    {id:3,question:"Find the bug:\n\ninterface Config {\n  readonly host: string;\n  port: number;\n}\nconst cfg: Config = { host: 'localhost', port: 3000 };\ncfg.host = 'newhost';",options:["No bug","Cannot assign to readonly 'host'","Missing property","Interface error"],answer:"Cannot assign to readonly 'host'",explanation:"readonly prevents modification after initialization. cfg.host = 'newhost' — Cannot assign to 'host' because it is a read-only property."},
    {id:4,question:"Find the bug:\n\nfunction merge<T>(a: T, b: T): T {\n  return { ...a, ...b };\n}",options:["No bug","Spread requires T to be object — unconstrained T might not be spreadable","Generic correct","Return error"],answer:"Spread requires T to be object — unconstrained T might not be spreadable",explanation:"Spread {...a, ...b} requires object types. Unconstrained T could be primitive. Use T extends object constraint."},
    {id:5,question:"Find the bug:\n\nconst identity = <T>(x: T) => x;\nconst s = identity<string>(42);",options:["No bug","Type argument 'string' but value '42' is number","Arrow generic","Wrong syntax"],answer:"Type argument 'string' but value '42' is number",explanation:"identity<string>(42) — explicitly says T=string but 42 is number. Argument of type 'number' not assignable to parameter type 'string'."},
    {id:6,question:"Find the bug:\n\ntype Admin = { role: 'admin'; permissions: string[] };\ntype User = { role: 'user'; name: string };\ntype Person = Admin | User;\n\nfunction handle(p: Person) {\n  console.log(p.permissions);\n}",options:["No bug","permissions not on User — need to narrow first","Union error","Wrong type"],answer:"permissions not on User — need to narrow first",explanation:"permissions only on Admin. Without narrowing, accessing p.permissions errors — Property 'permissions' does not exist on type 'User'."},
    {id:7,question:"Find the bug:\n\ninterface Printable {\n  print(): void;\n}\ninterface Serializable {\n  serialize(): string;\n}\nfunction process<T extends Printable>(item: T) {\n  item.print();\n  item.serialize();\n}",options:["No bug","serialize() not in Printable constraint — need T extends Printable & Serializable","Generic error","Interface missing"],answer:"serialize() not in Printable constraint — need T extends Printable & Serializable",explanation:"T extends Printable guarantees only print(). serialize() not guaranteed. Use T extends Printable & Serializable."},
    {id:8,question:"Find the bug:\n\nconst users: Array<{ name: string }> = [\n  { name: 'Ali' },\n  { name: 'Sara', age: 30 }\n];",options:["No bug","Excess property — 'age' not in { name: string }","Array error","Generic mismatch"],answer:"Excess property — 'age' not in { name: string }",explanation:"Array element { name: 'Sara', age: 30 } has excess property age. Object literal check — 'age' does not exist in type '{ name: string }'."},
    {id:9,question:"Find the bug:\n\ntype Readonly<T> = { readonly [P in keyof T]: T[P] };\ntype User = { name: string; age: number };\nconst u: Readonly<User> = { name: 'Ali', age: 25 };\nu.age++;",options:["No bug","Cannot increment readonly 'age'","Type error","Mapped type error"],answer:"Cannot increment readonly 'age'",explanation:"Readonly<User> makes all properties readonly. u.age++ = u.age = u.age + 1 — Cannot assign to 'age' (read-only)."},
    {id:10,question:"Find the bug:\n\nfunction head<T>(arr: T[]): T {\n  if(arr.length === 0) return null;\n  return arr[0];\n}",options:["No bug","Return type T doesn't include null — should be T | null","Generic wrong","Array check wrong"],answer:"Return type T doesn't include null — should be T | null",explanation:"With strictNullChecks, null not assignable to T. Change return type to T | null or T | undefined."},
  ]},
  { id:"ts-bug-classes", name:"Bug: Classes & OOP", icon:"🐛", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\nclass Animal {\n  private name: string;\n  constructor(name: string) {\n    this.name = name;\n  }\n}\nclass Dog extends Animal {\n  bark() { return `${this.name} barks`; }\n}",options:["No bug","'name' is private — not accessible in Dog subclass","Wrong extends","Constructor error"],answer:"'name' is private — not accessible in Dog subclass",explanation:"private members only accessible in declaring class. Dog cannot access this.name. Use protected for subclass access."},
    {id:2,question:"Find the bug:\n\nabstract class Shape {\n  abstract area(): number;\n}\nconst s = new Shape();",options:["No bug","Cannot instantiate abstract class directly","Missing implementation","Wrong new"],answer:"Cannot instantiate abstract class directly",explanation:"Abstract classes cannot be instantiated. new Shape() — Cannot create an instance of an abstract class."},
    {id:3,question:"Find the bug:\n\nclass MyClass {\n  static count: number;\n  constructor() {\n    this.count++;\n  }\n}",options:["No bug","Property 'count' is static — use MyClass.count not this.count","Missing initialization","Wrong constructor"],answer:"Property 'count' is static — use MyClass.count not this.count",explanation:"Static properties accessed via class name. this.count refers to instance — Property 'count' does not exist on instance. Use MyClass.count++."},
    {id:4,question:"Find the bug:\n\nclass Vehicle {\n  constructor(public speed: number) {}\n}\nclass Car extends Vehicle {\n  constructor(speed: number, public brand: string) {\n    this.brand = brand;\n    super(speed);\n  }\n}",options:["No bug","super() must be called before 'this' — move super(speed) first","Wrong constructor","Brand error"],answer:"super() must be called before 'this' — move super(speed) first",explanation:"In derived class, super() must be called before accessing this. this.brand = brand before super(speed) — ReferenceError."},
    {id:5,question:"Find the bug:\n\nclass Timer {\n  private ticks = 0;\n  start() {\n    setInterval(function() {\n      this.ticks++;\n    }, 1000);\n  }\n}",options:["No bug","'this' is undefined in regular function callback — use arrow function","Wrong setInterval","ticks error"],answer:"'this' is undefined in regular function callback — use arrow function",explanation:"Regular function has its own this — undefined in strict mode. Use arrow: setInterval(() => { this.ticks++; }, 1000)."},
    {id:6,question:"Find the bug:\n\ninterface Flyable { fly(): void; }\ninterface Swimmable { swim(): void; }\n\nclass Duck implements Flyable {\n  fly() { console.log('flying'); }\n}",options:["No bug — only Flyable needed","No bug","Duck should implement both Flyable and Swimmable","Wrong class"],answer:"No bug",explanation:"Duck only implements Flyable — that's valid. A class doesn't have to implement all related interfaces. No bug here."},
    {id:7,question:"Find the bug:\n\nclass Singleton {\n  private static instance: Singleton;\n  constructor() {}\n  static getInstance() {\n    if(!Singleton.instance)\n      Singleton.instance = new Singleton();\n    return Singleton.instance;\n  }\n}",options:["Logic wrong","Constructor is public — new Singleton() bypasses getInstance()","Static error","Return error"],answer:"Constructor is public — new Singleton() bypasses getInstance()",explanation:"Singleton needs private constructor. Public constructor allows multiple instances — defeats Singleton pattern."},
    {id:8,question:"Find the bug:\n\nclass Stack<T> {\n  items: T[] = [];\n  push(item: T) { this.items.push(item); }\n  pop(): T { return this.items.pop(); }\n}",options:["No bug","pop() return type is T but items.pop() returns T | undefined","Generic error","Array error"],answer:"pop() return type is T but items.pop() returns T | undefined",explanation:"Array.pop() returns T | undefined (empty array case). Return type T is wrong — should be T | undefined."},
    {id:9,question:"Find the bug:\n\nclass EventEmitter {\n  private handlers: Map<string, Function[]> = new Map();\n  on(event: string, handler: Function) {\n    this.handlers.get(event).push(handler);\n  }\n}",options:["No bug","handlers.get() returns undefined if key missing — push on undefined crashes","Map error","Function type"],answer:"handlers.get() returns undefined if key missing — push on undefined crashes",explanation:"Map.get() returns T | undefined. If event key doesn't exist, .push on undefined throws. Check/initialize first."},
    {id:10,question:"Find the bug:\n\nclass Person {\n  readonly name: string;\n  constructor(name: string) {\n    this.name = name;\n  }\n  changeName(newName: string) {\n    this.name = newName;\n  }\n}",options:["No bug","Cannot assign to readonly 'name' outside constructor","Wrong method","Readonly ok in class"],answer:"Cannot assign to readonly 'name' outside constructor",explanation:"readonly can only be assigned in constructor or declaration. changeName() tries to reassign — Cannot assign to 'name' (read-only)."},
  ]},
  { id:"ts-bug-advanced", name:"Bug: Advanced", icon:"🐛", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\nfunction process(value: string | number) {\n  return value.toUpperCase();\n}",options:["No bug","toUpperCase() not on 'number' — need type narrowing","Wrong method","Union error"],answer:"toUpperCase() not on 'number' — need type narrowing",explanation:"toUpperCase is string-only. On string|number, TypeScript errors. Use: if(typeof value === 'string') value.toUpperCase()."},
    {id:2,question:"Find the bug:\n\nconst arr = [1, 2, 3];\nconst first = arr[0];\nfirst.toFixed(2);",options:["No bug","arr[0] type is number — toFixed valid. No bug","Type error","Array index"],answer:"No bug",explanation:"arr inferred as number[]. arr[0] is number. toFixed(2) is valid number method. No TypeScript error here."},
    {id:3,question:"Find the bug:\n\ntype Obj = Record<string, number>;\nconst obj: Obj = { a: 1, b: 2 };\nconst val: number = obj['c'];",options:["No bug","With noUncheckedIndexedAccess, obj['c'] is number|undefined not number","Key error","Wrong type"],answer:"With noUncheckedIndexedAccess, obj['c'] is number|undefined not number",explanation:"With strict settings, index access on Record returns T | undefined. Assigning to number without check is unsafe."},
    {id:4,question:"Find the bug:\n\nfunction getUser(): { name: string } | null {\n  return null;\n}\nconst user = getUser();\nconsole.log(user.name);",options:["No bug","Object possibly null — need null check before .name","Wrong return","Null type"],answer:"Object possibly null — need null check before .name",explanation:"getUser() can return null. user.name on null throws runtime error. TypeScript errors: Object is possibly 'null'."},
    {id:5,question:"Find the bug:\n\nconst map = new Map<string, number>();\nmap.set('a', 1);\nconst val: number = map.get('a');",options:["No bug","Map.get() returns number|undefined — not assignable to number","Map type error","Wrong get"],answer:"Map.get() returns number|undefined — not assignable to number",explanation:"Map.get() returns V | undefined — key might not exist. Type 'number | undefined' not assignable to 'number'."},
    {id:6,question:"Find the bug:\n\nenum Color { Red, Green, Blue }\nfunction getColor(c: Color): string {\n  switch(c) {\n    case Color.Red: return 'red';\n    case Color.Green: return 'green';\n  }\n}",options:["No bug","Missing return — Color.Blue not handled — function may return undefined","Enum error","Switch error"],answer:"Missing return — Color.Blue not handled — function may return undefined",explanation:"Switch doesn't cover Color.Blue — function may return undefined. Add case Color.Blue or default branch."},
    {id:7,question:"Find the bug:\n\nconst a = { x: 1 };\nconst b = { x: 2, y: 3 };\nconst c: typeof a = b;",options:["No bug","Excess property check — 'y' exists on b but not on typeof a","Type correct","Wrong typeof"],answer:"No bug",explanation:"typeof a = { x: number }. b has { x: number, y: number }. b is assignable to { x: number } — structural typing. No excess property error here (not object literal)."},
    {id:8,question:"Find the bug:\n\nasync function fetchData(): Promise<string> {\n  const result = await fetch('/api');\n  return result;\n}",options:["No bug","result is Response not string — need result.text() or result.json()","Await error","Promise error"],answer:"result is Response not string — need result.text() or result.json()",explanation:"fetch() returns Promise<Response>. await gives Response object, not string. Use return result.text() or result.json()."},
    {id:9,question:"Find the bug:\n\ntype Keys = 'a' | 'b' | 'c';\nconst obj: Record<Keys, number> = { a: 1, b: 2 };",options:["No bug","Property 'c' missing in Record<Keys, number>","Wrong Record","Key error"],answer:"Property 'c' missing in Record<Keys, number>",explanation:"Record<Keys, number> requires all keys. 'c' is missing — Property 'c' is missing in type '{ a: number; b: number; }'."},
    {id:10,question:"Find the bug:\n\ninterface Config {\n  debug: boolean;\n}\ndeclare const config: Config;\nif(config.debug === 'true') {\n  console.log('debug mode');\n}",options:["No bug","'string' compared to 'boolean' — always false","Wrong if","Config error"],answer:"'string' compared to 'boolean' — always false",explanation:"config.debug is boolean, 'true' is string. This condition is always false — TypeScript warns: comparison appears to be unintentional."},
  ]},
];

// ══════════════════════════════════════════════════
// TYPESCRIPT — FILL IN THE BLANK (⬜)
// ══════════════════════════════════════════════════
export const TS_FILL_TOPICS: Topic[] = [
  { id:"ts-fill-types", name:"Fill: Types & Interfaces", icon:"⬜", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"Complete the type annotation:\n\nlet name: _____ = 'Ali';\nlet age: _____ = 25;\nlet active: _____ = true;",options:["string / number / boolean","String / Number / Boolean","str / num / bool","any / any / any"],answer:"string / number / boolean",explanation:"TypeScript primitive types: string, number, boolean — all lowercase. Not String/Number/Boolean (wrapper objects)."},
    {id:2,question:"Complete the interface:\n\ninterface User {\n  name: string;\n  age_____: number; // optional\n  readonly id: _____; // cannot change\n}",options:["? / number","! / string","= / number","* / any"],answer:"? / number",explanation:"? makes property optional. readonly prevents modification. id: number type given context is ID."},
    {id:3,question:"Complete the union type:\n\ntype StringOrNumber = string _____ number;\nlet x: StringOrNumber = 'hello';\nx = 42; // also valid",options:["|","&","+","||"],answer:"|",explanation:"| creates union type — value can be any of the listed types. & creates intersection (must satisfy all)."},
    {id:4,question:"Complete the function with types:\n\nfunction add(a: number, b: number): _____ {\n  return a + b;\n}",options:["void","number","string","any"],answer:"number",explanation:"Function adds two numbers — return type is number. TypeScript can infer this but explicit annotation is clear."},
    {id:5,question:"Complete the array type:\n\nconst nums: _____[] = [1, 2, 3];\nconst strs: Array<_____> = ['a', 'b'];",options:["number / string","Number / String","int / str","num / str"],answer:"number / string",explanation:"number[] and Array<string> are equivalent array type syntaxes. Lowercase primitive types."},
    {id:6,question:"Complete the enum:\n\n_____ Color {\n  Red,\n  Green,\n  Blue\n}\nconsole.log(Color.Red); // 0",options:["type","interface","enum","class"],answer:"enum",explanation:"enum keyword creates named constants. Numeric enum starts at 0 by default."},
    {id:7,question:"Complete the type alias:\n\n_____ Point = {\n  x: number;\n  y: number;\n};\nconst p: Point = { x: 1, y: 2 };",options:["interface","class","type","const"],answer:"type",explanation:"type keyword creates type alias. interface also works for objects but type is more flexible (union, intersection, primitives)."},
    {id:8,question:"Complete the readonly array:\n\nconst arr: _____ number[] = [1, 2, 3];\n// arr.push(4); // Error!",options:["const","static","readonly","immutable"],answer:"readonly",explanation:"readonly number[] (or ReadonlyArray<number>) prevents mutation. const only prevents reassignment of variable."},
    {id:9,question:"Complete the tuple:\n\nconst pair: [string, _____] = ['hello', 42];",options:["any","string","number","Number"],answer:"number",explanation:"Tuple [string, number] — first element string, second number. pair[1] type is number."},
    {id:10,question:"Complete the intersection type:\n\ntype Admin = { role: string };\ntype User = { name: string };\ntype AdminUser = Admin _____ User;\nconst au: AdminUser = { role: 'admin', name: 'Ali' };",options:["|","&","+","&&"],answer:"&",explanation:"& creates intersection — AdminUser must have all properties of both Admin AND User."},
  ]},
  { id:"ts-fill-generics", name:"Fill: Generics & Utility Types", icon:"⬜", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"Complete the generic function:\n\nfunction identity<_____>(value: T): T {\n  return value;\n}",options:["any","T","Type","V"],answer:"T",explanation:"T is conventional type parameter name. <T> declares it, (value: T): T uses it. Could be any name but T is standard."},
    {id:2,question:"Complete to make all properties optional:\n\ninterface User { name: string; age: number; }\ntype PartialUser = _____<User>;",options:["Optional","Required","Partial","Readonly"],answer:"Partial",explanation:"Partial<T> makes all properties optional. Built-in utility type equivalent to { [P in keyof T]?: T[P] }."},
    {id:3,question:"Complete to make all properties required:\n\ntype Config = { host?: string; port?: number; };\ntype RequiredConfig = _____<Config>;",options:["Partial","Required","Readonly","NonNull"],answer:"Required",explanation:"Required<T> removes optional modifier — all properties become required. Opposite of Partial<T>."},
    {id:4,question:"Complete to pick specific properties:\n\ninterface User { name: string; age: number; email: string; }\ntype UserPreview = _____<User, 'name' | 'email'>;",options:["Omit","Pick","Extract","Select"],answer:"Pick",explanation:"Pick<T, K> creates type with only specified properties. UserPreview has only name and email."},
    {id:5,question:"Complete to exclude properties:\n\ninterface User { name: string; password: string; email: string; }\ntype PublicUser = _____<User, 'password'>;",options:["Pick","Exclude","Omit","Remove"],answer:"Omit",explanation:"Omit<T, K> removes specified properties. PublicUser has name and email but not password."},
    {id:6,question:"Complete the Record type:\n\ntype Scores = _____<string, number>;\nconst s: Scores = { math: 90, english: 85 };",options:["Map","Dict","Record","Object"],answer:"Record",explanation:"Record<K, V> creates object type with keys K and values V. Record<string, number> = { [key: string]: number }."},
    {id:7,question:"Complete to extract return type:\n\nfunction getUser() { return { name: 'Ali', age: 25 }; }\ntype UserType = _____<typeof getUser>;",options:["InstanceType","ReturnType","Parameters","typeof"],answer:"ReturnType",explanation:"ReturnType<T> extracts return type of function T. ReturnType<typeof getUser> = { name: string; age: number }."},
    {id:8,question:"Complete the constrained generic:\n\nfunction getLength<T _____ { length: number }>(x: T): number {\n  return x.length;\n}",options:["implements","extends","is","has"],answer:"extends",explanation:"extends constrains generic T. T extends { length: number } means T must have length property. Allows arrays, strings."},
    {id:9,question:"Complete to exclude from union:\n\ntype All = string | number | boolean | null;\ntype NoNull = _____<All, null>;",options:["Omit","Remove","Exclude","Pick"],answer:"Exclude",explanation:"Exclude<T, U> removes from union T the types assignable to U. Exclude<string|number|null, null> = string|number."},
    {id:10,question:"Complete NonNullable:\n\ntype MaybeString = string | null | undefined;\ntype DefiniteString = _____<MaybeString>;",options:["Required","Defined","NonNullable","NotNull"],answer:"NonNullable",explanation:"NonNullable<T> removes null and undefined from type. NonNullable<string|null|undefined> = string."},
  ]},
  { id:"ts-fill-advanced", name:"Fill: Advanced Patterns", icon:"⬜", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Complete the type guard:\n\nfunction isString(value: unknown): value _____ string {\n  return typeof value === 'string';\n}",options:["is","as","extends","instanceof"],answer:"is",explanation:"is keyword creates type predicate. value is string narrows type in branches where function returns true."},
    {id:2,question:"Complete the mapped type:\n\ntype Nullable<T> = {\n  [P _____ keyof T]: T[P] | null;\n};",options:["extends","in","of","from"],answer:"in",explanation:"[P in keyof T] iterates over all property keys of T. Creates new type where each property can also be null."},
    {id:3,question:"Complete the conditional type:\n\ntype IsArray<T> = T _____ any[] ? true : false;\ntype R = IsArray<string[]>; // true",options:["extends","implements","is","==="],answer:"extends",explanation:"Conditional type uses extends for the check. T extends any[] checks if T is array type. Returns true or false type."},
    {id:4,question:"Complete the infer keyword:\n\ntype UnpackPromise<T> = T extends Promise<_____ U> ? U : T;",options:["type","extends","infer","keyof"],answer:"infer",explanation:"infer captures type within conditional. infer U in Promise<infer U> extracts the Promise's resolved type."},
    {id:5,question:"Complete the template literal type:\n\ntype EventName<T extends string> = `on${_____<T>}`;\ntype ClickEvent = EventName<'click'>; // 'onClick'",options:["Upper","Uppercase","Capitalize","ToUpperCase"],answer:"Capitalize",explanation:"Capitalize<S> uppercases first letter. Capitalize<'click'> = 'Click'. Template literal: `on${'Click'}` = 'onClick'."},
    {id:6,question:"Complete the discriminated union:\n\ntype Shape =\n  | { _____ : 'circle'; radius: number }\n  | { _____ : 'rect'; width: number; height: number };\n\nfunction area(s: Shape) {\n  if(s.kind === 'circle') return Math.PI * s.radius ** 2;\n}",options:["type / type","kind / kind","shape / shape","name / name"],answer:"kind / kind",explanation:"Discriminated union uses common literal field (kind) to distinguish members. TypeScript narrows on s.kind check."},
    {id:7,question:"Complete the decorator:\n\nfunction log(target: any, key: string, descriptor: PropertyDescriptor) {\n  const original = descriptor.value;\n  descriptor.value = function(...args: any[]) {\n    console.log(`Calling ${key}`);\n    return _____._____(_______);\n  };\n  return descriptor;\n}",options:["original.call(this, ...args)","original / call / this","original.apply / this / args","original / apply / this"],answer:"original.call(this, ...args)",explanation:"original.call(this, ...args) calls the original method with correct this context and spread arguments."},
    {id:8,question:"Complete the assertion function:\n\nfunction assert(condition: boolean, msg: string): _____ condition {\n  if(!condition) throw new Error(msg);\n}",options:["is","asserts","ensures","checks"],answer:"asserts",explanation:"asserts condition tells TypeScript that after this function returns, condition is true — narrows type in caller."},
    {id:9,question:"Complete the module augmentation:\n\ndeclare module 'express' {\n  interface _____ {\n    user?: { id: string };\n  }\n}",options:["Response","Request","Router","Application"],answer:"Request",explanation:"Augmenting Express Request interface to add custom user property — common pattern for auth middleware typing."},
    {id:10,question:"Complete the satisfies operator:\n\ntype Colors = Record<string, [number,number,number] | string>;\nconst palette = {\n  red: [255, 0, 0],\n  blue: '#0000ff'\n} _____ Colors;",options:["as","extends","satisfies","implements"],answer:"satisfies",explanation:"satisfies validates type without widening. palette.red stays [number,number,number], not widened to array|string. TypeScript 4.9+."},
  ]},
  { id:"ts-fill-classes", name:"Fill: Classes & Decorators", icon:"⬜", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Complete constructor shorthand:\n\nclass Point {\n  constructor(\n    _____ x: number,\n    _____ y: number\n  ) {}\n}",options:["public / public","private / private","readonly / readonly","public / private"],answer:"public / public",explanation:"Access modifier in constructor parameter auto-creates and assigns property. public x: number = this.x = x."},
    {id:2,question:"Complete the abstract class:\n\n_____ class Animal {\n  _____ speak(): string;\n  describe() { return `I say ${this.speak()}`; }\n}",options:["abstract / abstract","interface / abstract","abstract / virtual","class / abstract"],answer:"abstract / abstract",explanation:"abstract class cannot be instantiated. abstract method has no body — subclasses must implement it."},
    {id:3,question:"Complete the static factory:\n\nclass Color {\n  private constructor(public hex: string) {}\n  _____ fromRGB(r: number, g: number, b: number): Color {\n    return new Color(`#${r.toString(16)}${g.toString(16)}${b.toString(16)}`);\n  }\n}",options:["public","private","static","readonly"],answer:"static",explanation:"static method on class — called as Color.fromRGB(). Private constructor + static factory = controlled creation."},
    {id:4,question:"Complete the generic class:\n\nclass Repository<_____> {\n  private items: T[] = [];\n  add(item: T) { this.items.push(item); }\n  getAll(): T[] { return this.items; }\n}",options:["any","Type","T","V"],answer:"T",explanation:"T is type parameter. Repository<T> makes class generic — Repository<User>, Repository<Product> etc."},
    {id:5,question:"Complete the interface implementation:\n\ninterface Serializable {\n  serialize(): string;\n  deserialize(data: string): _____;\n}\nclass JsonSerializer implements Serializable {\n  serialize() { return '{}'; }\n  deserialize(data: string) { return JSON.parse(data); }\n}",options:["string","void","any","object"],answer:"any",explanation:"deserialize returns parsed JSON — unknown structure. Return type any or unknown. The interface declares this flexibility."},
    {id:6,question:"Complete the mixin function:\n\ntype Constructor<T = {}> = new (...args: any[]) => T;\n\nfunction Timestamped<TBase extends _____>(Base: TBase) {\n  return class extends Base {\n    createdAt = new Date();\n  };\n}",options:["object","Constructor","{}","any"],answer:"Constructor",explanation:"Constructor type constraint ensures TBase is a constructable class. Mixin pattern extends any base class."},
    {id:7,question:"Complete the property decorator:\n\nfunction readonly(target: any, propertyKey: string) {\n  Object.defineProperty(target, propertyKey, {\n    _____: false,\n    configurable: false\n  });\n}",options:["editable","changeable","writable","mutable"],answer:"writable",explanation:"Object.defineProperty with writable: false makes property read-only at runtime. Property descriptor attribute."},
    {id:8,question:"Complete using declare:\n\nclass MyClass {\n  _____ name: string;\n  constructor(name: string) {\n    this.name = name;\n  }\n}",options:["public","declare","private","readonly"],answer:"declare",explanation:"declare tells TypeScript property exists without emitting JS. Used when property assigned in constructor — avoids strict class property check."},
    {id:9,question:"Complete the override keyword:\n\nclass Base {\n  greet() { return 'Hello'; }\n}\nclass Child extends Base {\n  _____ greet() { return 'Hi'; }\n}",options:["virtual","abstract","override","super"],answer:"override",explanation:"override (TypeScript 4.3+) explicitly marks method as overriding base class method. Error if base doesn't have matching method."},
    {id:10,question:"Complete this parameter type:\n\nclass EventEmitter {\n  on(event: string, handler: (_____ this: void, data: any) => void) {\n    // register handler\n  }\n}",options:["readonly","const","fake","this"],answer:"this",explanation:"this: void in parameter constrains how function is called — handler cannot use this. Type-only parameter, not real argument."},
  ]},
];