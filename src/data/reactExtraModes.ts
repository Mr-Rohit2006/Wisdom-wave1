import type { Topic } from "./arcadeQuestions";

// ══════════════════════════════════════════════════
// REACT — OUTPUT GUESS (💻)
// ══════════════════════════════════════════════════
export const REACT_OUTPUT_TOPICS: Topic[] = [
  { id:"react-out-hooks", name:"Output: Hooks", icon:"💻", difficulty:"Medium", category:"Hooks", questions:[
    {id:1,question:"What is the output on first render?\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  console.log(count);\n  return <button onClick={() => setCount(count + 1)}>{count}</button>;\n}",options:["0","1","undefined","Error"],answer:"0",explanation:"useState(0) initializes count to 0. On first render, console.log(count) prints 0."},
    {id:2,question:"How many times does useEffect run?\n\nfunction App() {\n  const [a, setA] = useState(0);\n  const [b, setB] = useState(0);\n  useEffect(() => {\n    console.log('effect');\n  }, [a]);\n  // setA called 2 times, setB called 3 times\n}",options:["5","3","2","1"],answer:"2",explanation:"useEffect with [a] dependency runs when a changes. setA called 2 times → effect runs 2 times. setB changes don't trigger it."},
    {id:3,question:"What is the output?\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  useEffect(() => {\n    console.log(count);\n  });\n  setCount(1);\n}",options:["0","1","0 then 1","Infinite loop"],answer:"Infinite loop",explanation:"useEffect with no dependency array runs after every render. setCount triggers re-render, which triggers effect, which causes re-render..."},
    {id:4,question:"What does useRef return on first render?\n\nfunction App() {\n  const ref = useRef(10);\n  console.log(ref);\n}",options:["10","{ current: 10 }","undefined","null"],answer:"{ current: 10 }",explanation:"useRef returns object with current property. useRef(10) → { current: 10 }. Mutating ref.current doesn't trigger re-render."},
    {id:5,question:"What is the output?\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const handleClick = () => {\n    setCount(count + 1);\n    setCount(count + 1);\n  };\n  // after one click\n  console.log(count);\n}",options:["2","1","0","Error"],answer:"1",explanation:"Both setCount calls use same stale count=0. count+1=1 both times. State batched — count becomes 1, not 2."},
    {id:6,question:"What is the output?\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const handleClick = () => {\n    setCount(c => c + 1);\n    setCount(c => c + 1);\n  };\n  // after one click\n  console.log(count);\n}",options:["2","1","0","Error"],answer:"2",explanation:"Functional updates use latest state. First: 0→1. Second: 1→2. Both queued correctly → count = 2."},
    {id:7,question:"What is the output?\n\nfunction App() {\n  const [user, setUser] = useState({ name: 'Ali', age: 25 });\n  setUser({ name: 'Sara' });\n  console.log(user.age);\n}",options:["25","undefined","Sara","Error"],answer:"undefined",explanation:"useState doesn't merge — it replaces. setUser({ name: 'Sara' }) replaces entire object. age is gone → undefined."},
    {id:8,question:"What is the output?\n\nfunction App() {\n  const value = useMemo(() => {\n    return 2 + 2;\n  }, []);\n  console.log(value);\n}",options:["() => 2+2","4","undefined","Error"],answer:"4",explanation:"useMemo executes the function and returns the result. With [] deps, computed once. value = 4."},
    {id:9,question:"What is the output?\n\nconst Child = React.memo(({ count }) => {\n  console.log('render');\n  return <div>{count}</div>;\n});\n// Parent re-renders with same count=5",options:["render printed","render NOT printed","Error","undefined"],answer:"render NOT printed",explanation:"React.memo skips re-render if props unchanged. Same count=5 → Child doesn't re-render → 'render' not printed."},
    {id:10,question:"What is the output?\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  useEffect(() => {\n    return () => console.log('cleanup', count);\n  }, [count]);\n  // count changes from 0 to 1\n}",options:["cleanup 1","cleanup 0","cleanup undefined","No output"],answer:"cleanup 0",explanation:"Cleanup runs with the OLD value before effect re-runs. When count changes 0→1, cleanup logs 'cleanup 0'."},
  ]},
  { id:"react-out-rendering", name:"Output: Rendering", icon:"💻", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"What renders?\n\nfunction App() {\n  return (\n    <div>\n      {true && <span>Hello</span>}\n      {false && <span>World</span>}\n    </div>\n  );\n}",options:["Hello World","Hello","World","Nothing"],answer:"Hello",explanation:"true && <span> renders the span. false && <span> short-circuits — false not rendered. Only Hello shown."},
    {id:2,question:"What renders?\n\nfunction App() {\n  return (\n    <div>\n      {0 && <span>Hello</span>}\n    </div>\n  );\n}",options:["Hello","Nothing","0","Error"],answer:"0",explanation:"0 is falsy but React renders 0 as text node! Use Boolean(0) && or !!0 && to avoid. Common React gotcha."},
    {id:3,question:"What is the output?\n\nfunction App() {\n  const items = ['a', 'b', 'c'];\n  return items.map(item => <li>{item}</li>);\n}",options:["<li>a</li><li>b</li><li>c</li>","Error — missing key","Renders with warning","Nothing"],answer:"Renders with warning",explanation:"Missing key prop causes React warning but still renders. Each list item should have unique key prop."},
    {id:4,question:"What is the output?\n\nfunction App({ name }) {\n  return <div>Hello {name ?? 'World'}</div>;\n}\n<App />",options:["Hello undefined","Hello World","Hello null","Error"],answer:"Hello World",explanation:"name prop not passed → undefined. ?? (nullish coalescing) returns 'World' for undefined. Renders Hello World."},
    {id:5,question:"What renders?\n\nfunction App() {\n  return (\n    <>\n      <h1>Title</h1>\n      <p>Para</p>\n    </>\n  );\n}",options:["Error — multiple roots","<div> wraps them","h1 and p rendered","Fragment not valid"],answer:"h1 and p rendered",explanation:"<> </> is React Fragment. Renders multiple elements without extra DOM node. Both h1 and p render."},
    {id:6,question:"What is the output?\n\nconst El = 'div';\nreturn <El className='box'>Hello</El>;",options:["Error","<div class='box'>Hello</div>","<El>Hello</El>","undefined"],answer:"<div class='box'>Hello</div>",explanation:"JSX with capital variable holding string tag renders that HTML element. El='div' → renders <div>."},
    {id:7,question:"What is the output?\n\nfunction App() {\n  return React.createElement('h1', { id: 'title' }, 'Hello');\n}",options:["Error","<h1 id='title'>Hello</h1>","createElement not valid","Nothing"],answer:"<h1 id='title'>Hello</h1>",explanation:"React.createElement(type, props, children) is what JSX compiles to. Renders <h1 id='title'>Hello</h1>."},
    {id:8,question:"What renders?\n\nfunction App() {\n  const show = false;\n  return (\n    <div>\n      {show ? <p>Yes</p> : null}\n    </div>\n  );\n}",options:["Yes","null string","Empty div","Error"],answer:"Empty div",explanation:"null renders nothing in JSX. show=false → null → empty <div></div> rendered."},
    {id:9,question:"What is the output?\n\nfunction App() {\n  return <div style={{ color: 'red', fontSize: 16 }}>Hi</div>;\n}",options:["Error — string styles","<div style='color:red'>","<div> with red text size 16","style={{}} not valid"],answer:"<div> with red text size 16",explanation:"JSX style takes object with camelCase properties. fontSize not font-size. Renders div with red color, 16px font."},
    {id:10,question:"What is the output?\n\nfunction Greet({ name = 'World' }) {\n  return <h1>Hello {name}</h1>;\n}\n<Greet name={null} />",options:["Hello World","Hello null","Hello","Hello undefined"],answer:"Hello",explanation:"null is passed as name. Default parameter only triggers for undefined, not null. null renders as empty string. Output: Hello."},
  ]},
  { id:"react-out-state", name:"Output: State & Props", icon:"💻", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"What is the output after button click?\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const handleClick = () => {\n    console.log(count);\n    setCount(5);\n    console.log(count);\n  };\n  return <button onClick={handleClick}>Click</button>;\n}",options:["0 5","0 0","5 5","Error"],answer:"0 0",explanation:"setState is async — doesn't update immediately. Both console.logs see same stale count=0. New value available on next render."},
    {id:2,question:"What is the output?\n\nfunction Parent() {\n  const [val, setVal] = useState(1);\n  return <Child val={val} setVal={setVal} />;\n}\nfunction Child({ val, setVal }) {\n  console.log('Child render', val);\n  return <button onClick={() => setVal(2)}>Click</button>;\n}\n// After click",options:["Child render 1","Child render 2","Child render 1 then Child render 2","No output"],answer:"Child render 2",explanation:"After click, Parent re-renders with val=2, Child re-renders with new prop val=2. console.log prints 'Child render 2'."},
    {id:3,question:"What is the output?\n\nfunction App() {\n  const [items, setItems] = useState([1,2,3]);\n  const addItem = () => {\n    items.push(4);\n    setItems(items);\n  };\n  console.log(items.length);\n}",options:["3","4","Re-render happens","No re-render — same reference"],answer:"No re-render — same reference",explanation:"Mutating state array and setting same reference — React sees same object. No re-render triggered. Use [...items, 4] instead."},
    {id:4,question:"What is the output?\n\nfunction App() {\n  const [obj, setObj] = useState({ a: 1 });\n  useEffect(() => {\n    console.log(obj.a);\n  }, [obj]);\n  setObj({ a: 1 }); // same value, new object\n}",options:["1 once","1 infinitely","No output","Error"],answer:"1 infinitely",explanation:"setObj creates new object even with same value. New reference → obj changed → useEffect triggers → setObj again → infinite loop."},
    {id:5,question:"What is the output?\n\nfunction Child({ onClick }) {\n  console.log('render');\n  return <button onClick={onClick}>Click</button>;\n}\nconst MemoChild = React.memo(Child);\nfunction Parent() {\n  const [count, setCount] = useState(0);\n  const handleClick = () => setCount(c => c+1);\n  return <MemoChild onClick={handleClick} />;\n}\n// Parent re-renders",options:["render not printed","render printed — new function reference","Error","undefined"],answer:"render printed — new function reference",explanation:"handleClick is recreated on every Parent render — new reference. React.memo sees new prop → re-renders Child. Use useCallback to fix."},
    {id:6,question:"What is the output?\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const doubled = useMemo(() => count * 2, [count]);\n  setCount(3);\n  console.log(doubled);\n}",options:["0","3","6","Error"],answer:"6",explanation:"After setCount(3), count=3. useMemo recalculates: 3*2=6. doubled=6. console.log prints 6."},
    {id:7,question:"What is the output?\n\nfunction useCounter(init = 0) {\n  const [count, setCount] = useState(init);\n  const inc = () => setCount(c => c + 1);\n  return [count, inc];\n}\nfunction App() {\n  const [a, incA] = useCounter(5);\n  const [b, incB] = useCounter(10);\n  console.log(a, b);\n}",options:["0 0","5 10","5 0","Error"],answer:"5 10",explanation:"Custom hooks are independent instances. useCounter(5) → a=5, useCounter(10) → b=10. Each has own state."},
    {id:8,question:"What is the output?\n\nconst CountContext = createContext(0);\nfunction Child() {\n  const count = useContext(CountContext);\n  console.log(count);\n  return null;\n}\nfunction App() {\n  return (\n    <CountContext.Provider value={42}>\n      <Child />\n    </CountContext.Provider>\n  );\n}",options:["0","42","undefined","Error"],answer:"42",explanation:"useContext reads from nearest Provider above. Provider value=42. Child reads 42."},
    {id:9,question:"What is the output?\n\nfunction reducer(state, action) {\n  if (action.type === 'inc') return state + 1;\n  return state;\n}\nfunction App() {\n  const [count, dispatch] = useReducer(reducer, 0);\n  dispatch({ type: 'inc' });\n  dispatch({ type: 'inc' });\n  console.log(count);\n}",options:["0","1","2","Error"],answer:"0",explanation:"Like setState, dispatch is async. console.log sees stale count=0. Actual count=2 on next render."},
    {id:10,question:"What is the output?\n\nfunction App() {\n  const [x, setX] = useState(1);\n  useEffect(() => {\n    setX(2);\n  }, []);\n  console.log(x);\n}",options:["1","2","1 then 2","Error"],answer:"1 then 2",explanation:"First render: x=1, logs 1. After mount, useEffect runs: setX(2) triggers re-render. Second render: x=2, logs 2."},
  ]},
];

// ══════════════════════════════════════════════════
// REACT — BUG FINDER (🐛)
// ══════════════════════════════════════════════════
export const REACT_BUG_TOPICS: Topic[] = [
  { id:"react-bug-hooks", name:"Bug: Hooks", icon:"🐛", difficulty:"Medium", category:"Hooks", questions:[
    {id:1,question:"Find the bug:\n\nfunction App() {\n  if (someCondition) {\n    const [count, setCount] = useState(0);\n  }\n  return <div>{count}</div>;\n}",options:["No bug","Hooks inside conditionals — violates Rules of Hooks","useState wrong","condition error"],answer:"Hooks inside conditionals — violates Rules of Hooks",explanation:"Hooks must be called at top level, not inside conditionals/loops. Order must be same every render. Move useState outside if."},
    {id:2,question:"Find the bug:\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  useEffect(() => {\n    setCount(count + 1);\n  });\n}",options:["No bug","Infinite loop — no dependency array, setCount triggers re-render","setCount wrong","useEffect wrong"],answer:"Infinite loop — no dependency array, setCount triggers re-render",explanation:"No deps array → runs after every render. setCount → re-render → effect runs → setCount → infinite loop. Add [] or [count]."},
    {id:3,question:"Find the bug:\n\nfunction App() {\n  const [data, setData] = useState(null);\n  useEffect(async () => {\n    const res = await fetch('/api');\n    setData(await res.json());\n  }, []);\n}",options:["No bug","async useEffect returns Promise — use inner async function","fetch wrong","setData wrong"],answer:"async useEffect returns Promise — use inner async function",explanation:"useEffect callback must return cleanup function or nothing, not Promise. Create inner async function: const fetchData = async () => {...}; fetchData();"},
    {id:4,question:"Find the bug:\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const double = useCallback(() => count * 2, []);\n  return <div>{double()}</div>;\n}",options:["No bug","Stale closure — count not in deps, always returns 0","useCallback wrong","double() wrong"],answer:"Stale closure — count not in deps, always returns 0",explanation:"useCallback with [] captures count=0 forever. Add count to deps: useCallback(() => count * 2, [count])."},
    {id:5,question:"Find the bug:\n\nfunction App() {\n  const ref = useRef(null);\n  console.log(ref.current.value);\n  return <input ref={ref} />;\n}",options:["No bug","Error — ref.current is null before mount","useRef wrong","input wrong"],answer:"Error — ref.current is null before mount",explanation:"On first render, ref.current is null — DOM not mounted yet. Access ref.current inside useEffect or event handlers."},
    {id:6,question:"Find the bug:\n\nconst value = useMemo(() => {\n  return expensiveCalc();\n}, []);\n\nfunction App() {\n  return <div>{value}</div>;\n}",options:["No bug","Hooks must be inside component — not at module level","useMemo wrong","value wrong"],answer:"Hooks must be inside component — not at module level",explanation:"React Hooks can only be called inside functional components or custom hooks. useMemo at module level violates Rules of Hooks."},
    {id:7,question:"Find the bug:\n\nfunction App() {\n  const [items, setItems] = useState([]);\n  const addItem = () => {\n    setItems([...items, 'new']);\n    console.log(items.length);\n  };\n}",options:["No bug","Stale closure — items not updated yet at console.log","setItems wrong","spread wrong"],answer:"Stale closure — items not updated yet at console.log",explanation:"setItems schedules update — items not immediately updated. console.log sees old items length. Check length on next render."},
    {id:8,question:"Find the bug:\n\nfunction App() {\n  useEffect(() => {\n    const timer = setInterval(() => {\n      console.log('tick');\n    }, 1000);\n  }, []);\n}",options:["No bug","Memory leak — interval not cleared on unmount","setInterval wrong","useEffect wrong"],answer:"Memory leak — interval not cleared on unmount",explanation:"No cleanup returned. When component unmounts, interval keeps running. Return cleanup: return () => clearInterval(timer);"},
    {id:9,question:"Find the bug:\n\nfunction useData(url) {\n  const [data, setData] = useState(null);\n  fetch(url).then(r => r.json()).then(setData);\n  return data;\n}",options:["No bug","fetch called on every render — use useEffect","useState wrong","return wrong"],answer:"fetch called on every render — use useEffect",explanation:"fetch directly in function body runs on every render — infinite requests. Wrap in useEffect with [url] dependency."},
    {id:10,question:"Find the bug:\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const increment = useCallback(() => {\n    setCount(count + 1);\n  }, []);\n  return <button onClick={increment}>{count}</button>;\n}",options:["No bug","Stale closure — count always 0, button always sets to 1","useCallback wrong","onClick wrong"],answer:"Stale closure — count always 0, button always sets to 1",explanation:"count captured as 0 forever in useCallback. Use functional update: setCount(c => c+1) or add count to deps."},
  ]},
  { id:"react-bug-rendering", name:"Bug: Rendering & Keys", icon:"🐛", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\nfunction App() {\n  const [items, setItems] = useState([1,2,3]);\n  return (\n    <ul>\n      {items.map((item, index) => (\n        <li key={index}>{item}</li>\n      ))}\n    </ul>\n  );\n}",options:["No bug","Using index as key causes issues when list order changes","key wrong","map wrong"],answer:"Using index as key causes issues when list order changes",explanation:"Index as key causes incorrect re-renders when items reordered/deleted. Use stable unique IDs: key={item.id}."},
    {id:2,question:"Find the bug:\n\nfunction App() {\n  return (\n    <div>\n      <h1>Title</h1>\n      <p>Para</p>\n    </div>\n    <footer>Footer</footer>\n  );\n}",options:["No bug","JSX must have one parent — wrap in Fragment","div wrong","footer wrong"],answer:"JSX must have one parent — wrap in Fragment",explanation:"JSX must return single root element. Two siblings (div, footer) at top level → syntax error. Wrap in <> </> or <div>."},
    {id:3,question:"Find the bug:\n\nfunction App() {\n  const data = null;\n  return (\n    <div>\n      <p>{data.name}</p>\n    </div>\n  );\n}",options:["No bug","TypeError — cannot read property of null","null renders fine","data wrong"],answer:"TypeError — cannot read property of null",explanation:"data is null. data.name throws TypeError. Use optional chaining: data?.name or conditional render: {data && data.name}."},
    {id:4,question:"Find the bug:\n\nfunction Button({ onClick }) {\n  return <button onClick={onClick()}>Click</button>;\n}\n<Button onClick={() => console.log('clicked')} />",options:["No bug","onClick() calls function immediately on render","button wrong","prop wrong"],answer:"onClick() calls function immediately on render",explanation:"onClick={onClick()} calls the function during render, not on click. Use onClick={onClick} to pass reference."},
    {id:5,question:"Find the bug:\n\nfunction App() {\n  return (\n    <div class='container'>\n      <label for='name'>Name</label>\n      <input id='name' />\n    </div>\n  );\n}",options:["No bug","class and for are reserved — use className and htmlFor","div wrong","input wrong"],answer:"class and for are reserved — use className and htmlFor",explanation:"JSX uses className instead of class, htmlFor instead of for (reserved JS keywords). React warns about these."},
    {id:6,question:"Find the bug:\n\nfunction App() {\n  const style = 'color: red; font-size: 16px';\n  return <div style={style}>Hello</div>;\n}",options:["No bug","style must be object not string","div wrong","string style ok"],answer:"style must be object not string",explanation:"JSX style prop requires object: style={{ color: 'red', fontSize: '16px' }}. String style is not valid in JSX."},
    {id:7,question:"Find the bug:\n\nfunction Parent() {\n  return <Child onClick={handleClick} />;\n}\nfunction handleClick() {\n  console.log('clicked');\n}",options:["No bug","handleClick not defined in Parent scope — ReferenceError","Child wrong","onClick wrong"],answer:"No bug",explanation:"handleClick is defined at module level — accessible in Parent. No scope issue. This is valid — no bug."},
    {id:8,question:"Find the bug:\n\nfunction App() {\n  const [show, setShow] = useState(true);\n  return (\n    <div>\n      {show && <Modal />}\n      <button onClick={setShow(false)}>Hide</button>\n    </div>\n  );\n}",options:["No bug","setShow(false) called on render not click — use () => setShow(false)","Modal wrong","show wrong"],answer:"setShow(false) called on render not click — use () => setShow(false)",explanation:"onClick={setShow(false)} calls setShow during render. Fix: onClick={() => setShow(false)} passes function reference."},
    {id:9,question:"Find the bug:\n\nfunction List({ items }) {\n  return (\n    <ul>\n      {items.map(item => {\n        return <li key={item.id}>{item.name}</li>;\n      })}\n    </ul>\n  );\n}\n<List />",options:["No bug","items is undefined — missing prop causes map error","key wrong","return wrong"],answer:"items is undefined — missing prop causes map error",explanation:"items prop not passed → undefined. undefined.map throws TypeError. Use default: function List({ items = [] }) or check before mapping."},
    {id:10,question:"Find the bug:\n\nconst MemoComp = React.memo(function Comp({ obj }) {\n  return <div>{obj.name}</div>;\n});\nfunction App() {\n  return <MemoComp obj={{ name: 'Ali' }} />;\n}",options:["No bug","React.memo won't work — new object on every render","memo wrong","obj wrong"],answer:"React.memo won't work — new object on every render",explanation:"{ name: 'Ali' } creates new object reference every render. React.memo does shallow comparison — always re-renders. Use useMemo or move object outside."},
  ]},
  { id:"react-bug-performance", name:"Bug: Performance & Patterns", icon:"🐛", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const expensiveValue = (() => {\n    // heavy calculation\n    return count * 1000;\n  })();\n  return <div>{expensiveValue}</div>;\n}",options:["No bug","IIFE runs on every render — use useMemo","useState wrong","return wrong"],answer:"IIFE runs on every render — use useMemo",explanation:"IIFE executes on every render even if count unchanged. Use useMemo(() => count*1000, [count]) to memoize."},
    {id:2,question:"Find the bug:\n\nfunction App() {\n  const [a, setA] = useState(0);\n  const [b, setB] = useState(0);\n  useEffect(() => {\n    console.log(a + b);\n  }, [a]);\n}",options:["No bug","Missing b in deps — stale b value used","useEffect wrong","console wrong"],answer:"Missing b in deps — stale b value used",explanation:"b used inside effect but not in deps array — ESLint exhaustive-deps warning. b may be stale. Add b to deps: [a, b]."},
    {id:3,question:"Find the bug:\n\nfunction Child({ name }) {\n  return <div>{name}</div>;\n}\nfunction App() {\n  const [count, setCount] = useState(0);\n  return (\n    <div>\n      <Child name='Ali' />\n      <button onClick={() => setCount(c=>c+1)}>{count}</button>\n    </div>\n  );\n}",options:["Child always re-renders unnecessarily — use React.memo","No bug","Child wrong","button wrong"],answer:"Child always re-renders unnecessarily — use React.memo",explanation:"Child re-renders on every App state change even though name='Ali' never changes. Wrap in React.memo to skip unnecessary renders."},
    {id:4,question:"Find the bug:\n\nconst Context = createContext();\nfunction App() {\n  const [user, setUser] = useState({ name: 'Ali' });\n  const [theme, setTheme] = useState('dark');\n  return (\n    <Context.Provider value={{ user, theme }}>\n      <App />\n    </Context.Provider>\n  );\n}",options:["No bug","Any state change re-renders all consumers — split contexts","Context wrong","value wrong"],answer:"Any state change re-renders all consumers — split contexts",explanation:"Single context with user+theme — any change triggers all consumers. Split into UserContext and ThemeContext for better performance."},
    {id:5,question:"Find the bug:\n\nfunction App() {\n  const [items, setItems] = useState([]);\n  useEffect(() => {\n    fetch('/api/items')\n      .then(r => r.json())\n      .then(data => setItems(data));\n  }, []);\n  // Component might unmount before fetch completes\n}",options:["No bug","setState on unmounted component — add cleanup flag","fetch wrong","useEffect wrong"],answer:"setState on unmounted component — add cleanup flag",explanation:"If component unmounts before fetch resolves, setItems causes memory leak warning. Add: let mounted = true; and check before setItems."},
    {id:6,question:"Find the bug:\n\nfunction App() {\n  const [list, setList] = useState([1,2,3]);\n  const sorted = list.sort((a,b) => b-a);\n  return <div>{sorted.join(',')}</div>;\n}",options:["No bug","sort mutates state directly — use [...list].sort()","useState wrong","sort wrong"],answer:"sort mutates state directly — use [...list].sort()",explanation:"Array.sort() mutates original. Mutating state directly breaks React's immutability. Use spread: [...list].sort((a,b)=>b-a)."},
    {id:7,question:"Find the bug:\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  return (\n    <div>\n      {Array(count).fill(null).map((_, i) => (\n        <Item key={i} />\n      ))}\n    </div>\n  );\n}",options:["No bug","Index key causes remounting when count decreases","Array wrong","fill wrong"],answer:"Index key causes remounting when count decreases",explanation:"When count decreases, items with index keys don't map correctly. React may remount wrong items. Use stable unique keys."},
    {id:8,question:"Find the bug:\n\nfunction useWindowSize() {\n  const [size, setSize] = useState({ w: 0, h: 0 });\n  useEffect(() => {\n    const handler = () => setSize({ w: window.innerWidth, h: window.innerHeight });\n    window.addEventListener('resize', handler);\n  }, []);\n  return size;\n}",options:["No bug","Memory leak — event listener not removed on unmount","addEventListener wrong","useState wrong"],answer:"Memory leak — event listener not removed on unmount",explanation:"No cleanup: listener persists after unmount. Return cleanup: return () => window.removeEventListener('resize', handler);"},
    {id:9,question:"Find the bug:\n\nfunction App() {\n  const [data, setData] = useState(null);\n  if (!data) {\n    useState([]); // try to initialize differently\n  }\n  return <div>{data}</div>;\n}",options:["No bug","Conditional hook call — violates Rules of Hooks","useState wrong","if wrong"],answer:"Conditional hook call — violates Rules of Hooks",explanation:"Hooks cannot be called conditionally. Number of hook calls must be same every render. Initialize properly: useState(null)."},
    {id:10,question:"Find the bug:\n\nconst cache = {};\nfunction App({ id }) {\n  if (!cache[id]) {\n    cache[id] = fetchData(id); // sync\n  }\n  return <div>{cache[id]}</div>;\n}",options:["No bug","fetchData not awaited — returns Promise not value","cache wrong","id wrong"],answer:"fetchData not awaited — returns Promise not value",explanation:"fetchData(id) returns Promise. Rendering Promise object shows [object Promise]. Use useEffect+useState for async data fetching."},
  ]},
];

// ══════════════════════════════════════════════════
// REACT — FILL IN THE BLANK (⬜)
// ══════════════════════════════════════════════════
export const REACT_FILL_TOPICS: Topic[] = [
  { id:"react-fill-hooks", name:"Fill: Hooks", icon:"⬜", difficulty:"Medium", category:"Hooks", questions:[
    {id:1,question:"Complete useState:\n\nfunction App() {\n  const [count, _____] = useState(0);\n  return <button onClick={() => setCount(count+1)}>{count}</button>;\n}",options:["count","update","setCount","dispatch"],answer:"setCount",explanation:"useState returns [state, setter]. Convention: setX for state X. setCount updates count."},
    {id:2,question:"Complete useEffect cleanup:\n\nuseEffect(() => {\n  const timer = setInterval(fn, 1000);\n  _____ () => clearInterval(timer);\n}, []);",options:["cleanup","return","yield","send"],answer:"return",explanation:"useEffect cleanup: return a function. React calls it on unmount or before next effect run."},
    {id:3,question:"Complete useCallback:\n\nconst handleClick = _____((\n  () => setCount(c => c+1)\n), []);",options:["useMemo","useRef","useCallback","useEffect"],answer:"useCallback",explanation:"useCallback memoizes function reference. Prevents Child re-renders when passed as prop. useMemo memoizes values."},
    {id:4,question:"Complete useContext:\n\nconst ThemeContext = createContext('light');\nfunction App() {\n  const theme = _____(ThemeContext);\n  return <div className={theme}>Hello</div>;\n}",options:["useState","useRef","useContext","createContext"],answer:"useContext",explanation:"useContext(Context) reads current context value from nearest Provider above in tree."},
    {id:5,question:"Complete useReducer:\n\nconst [state, _____] = useReducer(reducer, { count: 0 });\ndispatch({ type: 'increment' });",options:["setState","setCount","action","dispatch"],answer:"dispatch",explanation:"useReducer returns [state, dispatch]. dispatch(action) sends action to reducer function to compute new state."},
    {id:6,question:"Complete useRef for DOM:\n\nfunction App() {\n  const inputRef = useRef(null);\n  const focus = () => inputRef._____.focus();\n  return <input ref={inputRef} />;\n}",options:["value","ref","current","element"],answer:"current",explanation:"useRef returns { current: ... }. After mount, ref.current = DOM element. inputRef.current.focus() focuses the input."},
    {id:7,question:"Complete useMemo:\n\nconst expensiveResult = _____((\n  () => heavyCalc(data)\n), [data]);",options:["useCallback","useEffect","useMemo","useState"],answer:"useMemo",explanation:"useMemo memoizes computed value. Recomputes only when [data] changes. useCallback memoizes functions, useMemo memoizes values."},
    {id:8,question:"Complete custom hook:\n\nfunction _____(url) {\n  const [data, setData] = useState(null);\n  useEffect(() => {\n    fetch(url).then(r=>r.json()).then(setData);\n  }, [url]);\n  return data;\n}",options:["fetchData","getData","useFetch","fetchHook"],answer:"useFetch",explanation:"Custom hooks must start with 'use' — React enforces this convention. useFetch is valid custom hook name."},
    {id:9,question:"Complete useImperativeHandle:\n\nconst Input = forwardRef((props, ref) => {\n  const inputRef = useRef();\n  _____(ref, () => ({\n    focus: () => inputRef.current.focus()\n  }));\n  return <input ref={inputRef} />;\n});",options:["useRef","useEffect","useImperativeHandle","useCallback"],answer:"useImperativeHandle",explanation:"useImperativeHandle customizes what ref exposes. Parent gets {focus} instead of raw DOM element."},
    {id:10,question:"Complete useLayoutEffect:\n\nfunction App() {\n  const [height, setHeight] = useState(0);\n  const ref = useRef();\n  _____(()=>{\n    setHeight(ref.current.offsetHeight);\n  }, []);\n  return <div ref={ref}>Content</div>;\n}",options:["useEffect","useMemo","useRef","useLayoutEffect"],answer:"useLayoutEffect",explanation:"useLayoutEffect fires synchronously after DOM mutations, before browser paint. Use for DOM measurements. useEffect fires after paint."},
  ]},
  { id:"react-fill-patterns", name:"Fill: Patterns & Performance", icon:"⬜", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Complete React.memo:\n\nconst Button = _____(function({ onClick, label }) {\n  return <button onClick={onClick}>{label}</button>;\n});",options:["React.lazy","React.forwardRef","React.memo","React.createRef"],answer:"React.memo",explanation:"React.memo HOC memoizes component. Skips re-render if props unchanged (shallow comparison)."},
    {id:2,question:"Complete forwardRef:\n\nconst Input = React._____(function(props, ref) {\n  return <input ref={ref} {...props} />;\n});",options:["memo","createRef","forwardRef","useRef"],answer:"forwardRef",explanation:"React.forwardRef allows parent to pass ref to child's DOM element. Second argument in render function receives ref."},
    {id:3,question:"Complete lazy loading:\n\nconst Modal = React._____((\n  () => import('./Modal')\n));\nfunction App() {\n  return (\n    <Suspense fallback={<div>Loading...</div>}>\n      <Modal />\n    </Suspense>\n  );\n}",options:["memo","lazy","import","dynamic"],answer:"lazy",explanation:"React.lazy() enables code splitting. Component loaded lazily when needed. Must be wrapped in Suspense with fallback."},
    {id:4,question:"Complete error boundary:\n\nclass ErrorBoundary extends React.Component {\n  state = { hasError: false };\n  static _____(error) {\n    return { hasError: true };\n  }\n  render() {\n    if (this.state.hasError) return <h1>Error!</h1>;\n    return this.props.children;\n  }\n}",options:["componentDidCatch","getDerivedStateFromError","getSnapshotBeforeUpdate","shouldComponentUpdate"],answer:"getDerivedStateFromError",explanation:"getDerivedStateFromError returns state update on error. componentDidCatch is for logging errors. Both can be used together."},
    {id:5,question:"Complete Context Provider:\n\nconst UserContext = createContext(null);\nfunction App() {\n  const [user, setUser] = useState({ name: 'Ali' });\n  return (\n    <UserContext._____ value={user}>\n      <Dashboard />\n    </UserContext._____>\n  );\n}",options:["Consumer","Provider","Context","Wrapper"],answer:"Provider",explanation:"Context.Provider wraps components that need access to context value. value prop passes data down tree."},
    {id:6,question:"Complete compound component:\n\nfunction Select({ children, onChange }) {\n  const [selected, setSelected] = useState(null);\n  return (\n    <SelectContext.Provider value={{ selected, setSelected, onChange }}>\n      {_____}\n    </SelectContext.Provider>\n  );\n}",options:["props","children","content","slots"],answer:"children",explanation:"Compound components render children inside Provider. Children (Option etc.) consume context directly."},
    {id:7,question:"Complete render prop:\n\nfunction MouseTracker({ _____ }) {\n  const [pos, setPos] = useState({ x:0, y:0 });\n  return (\n    <div onMouseMove={e => setPos({x:e.clientX, y:e.clientY})}>\n      {render(pos)}\n    </div>\n  );\n}",options:["component","children","render","content"],answer:"render",explanation:"Render prop pattern: function prop that returns JSX. MouseTracker calls render(pos) to render whatever parent wants."},
    {id:8,question:"Complete HOC:\n\nfunction withAuth(WrappedComponent) {\n  return function(props) {\n    const isAuth = useAuth();\n    if (!isAuth) return <Redirect to='/login' />;\n    return <_____ {...props} />;\n  };\n}",options:["Component","WrappedComponent","WithAuth","Base"],answer:"WrappedComponent",explanation:"HOC wraps component, adds behavior. Renders WrappedComponent with original props if authenticated. Spread {...props} passes all props through."},
    {id:9,question:"Complete Suspense data fetching:\n\nfunction App() {\n  return (\n    <React.Suspense fallback={<Spinner />}>\n      <_____ />\n    </React.Suspense>\n  );\n}\nfunction DataComponent() {\n  const data = use(dataPromise); // React 19\n  return <div>{data}</div>;\n}",options:["DataComponent","Spinner","Suspense","lazy"],answer:"DataComponent",explanation:"Suspense catches components that suspend (throw promises). DataComponent uses use() hook which suspends until promise resolves."},
    {id:10,question:"Complete useTransition:\n\nfunction App() {\n  const [isPending, startTransition] = _____();\n  const [query, setQuery] = useState('');\n  const handleChange = (e) => {\n    startTransition(() => {\n      setQuery(e.target.value);\n    });\n  };\n}",options:["useDeferredValue","useCallback","useTransition","useMemo"],answer:"useTransition",explanation:"useTransition marks state updates as non-urgent. UI stays responsive during heavy re-renders. Returns [isPending, startTransition]."},
  ]},
  { id:"react-fill-state", name:"Fill: State Management", icon:"⬜", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Complete immutable state update:\n\nconst [user, setUser] = useState({ name: 'Ali', age: 25 });\nconst updateAge = () => {\n  setUser(prev => ({ _____prev, age: 26 }));\n};",options:["...","*","&","#"],answer:"...",explanation:"Spread operator creates new object with all old properties, then override age. Immutable update pattern."},
    {id:2,question:"Complete array state add:\n\nconst [items, setItems] = useState([]);\nconst addItem = (item) => {\n  setItems(prev => [_____prev, item]);\n};",options:["...","*","&","prev,"],answer:"...",explanation:"[...prev, item] creates new array with all existing items plus new item. Never mutate state array directly."},
    {id:3,question:"Complete array state remove:\n\nconst [items, setItems] = useState([1,2,3,4]);\nconst removeItem = (id) => {\n  setItems(prev => prev._____(item => item !== id));\n};",options:["map","find","filter","reduce"],answer:"filter",explanation:"filter returns new array excluding item with matching id. Immutable removal pattern."},
    {id:4,question:"Complete useReducer:\n\nfunction reducer(state, action) {\n  switch(action._____) {\n    case 'increment': return { count: state.count + 1 };\n    case 'decrement': return { count: state.count - 1 };\n    default: return state;\n  }\n}",options:["name","payload","type","kind"],answer:"type",explanation:"Convention: action.type identifies action kind. action.payload carries data. switch on type to handle different actions."},
    {id:5,question:"Complete Context with reducer:\n\nconst [state, dispatch] = useReducer(reducer, initialState);\nreturn (\n  <AppContext.Provider value={{ state, _____ }}>\n    {children}\n  </AppContext.Provider>\n);",options:["reducer","setState","dispatch","update"],answer:"dispatch",explanation:"Provide both state and dispatch through context. Consumers read state and call dispatch(action) to update."},
    {id:6,question:"Complete optimistic update:\n\nconst addTodo = async (text) => {\n  const tempId = Date.now();\n  setTodos(prev => [...prev, { id: tempId, text, _____ }]);\n  try {\n    const saved = await api.add(text);\n    setTodos(prev => prev.map(t => t.id===tempId ? saved : t));\n  } catch {\n    setTodos(prev => prev.filter(t => t.id !== tempId));\n  }\n};",options:["loading: true","pending: true","optimistic: true","temp: true"],answer:"pending: true",explanation:"Add item immediately (optimistic) with pending flag. On success: replace with real data. On error: remove. Better UX."},
    {id:7,question:"Complete derived state:\n\nconst [items, setItems] = useState([...]);\nconst [filter, setFilter] = useState('all');\nconst filteredItems = _____((() => {\n  return filter === 'all' ? items : items.filter(i => i.done === (filter === 'done'));\n}), [items, filter]);",options:["useState","useEffect","useMemo","useCallback"],answer:"useMemo",explanation:"Derived state computed from existing state. useMemo caches result, recomputes only when items or filter changes."},
    {id:8,question:"Complete state initialization:\n\nconst [data, setData] = useState(() => {\n  const saved = localStorage.getItem('data');\n  return saved ? _____(saved) : [];\n});",options:["JSON.stringify","JSON.parse","eval","Object"],answer:"JSON.parse",explanation:"Lazy initialization: function passed to useState runs once. JSON.parse converts stored string back to object/array."},
    {id:9,question:"Complete batch updates (React 18):\n\nimport { flushSync } from 'react-dom';\nflushSync(() => {\n  setCount(c => c+1);\n  setFlag(true);\n});\n// After this line, DOM is _____",options:["pending","batched","updated","async"],answer:"updated",explanation:"flushSync forces synchronous DOM update. After flushSync block, DOM reflects latest state — useful for measuring DOM."},
    {id:10,question:"Complete useId:\n\nfunction FormField({ label }) {\n  const id = _____();\n  return (\n    <>\n      <label htmlFor={id}>{label}</label>\n      <input id={id} />\n    </>\n  );\n}",options:["useState","useRef","useId","useMemo"],answer:"useId",explanation:"useId generates stable unique IDs that match between server and client renders. Perfect for accessibility attributes like htmlFor/id pairs."},
  ]},
];