import type { Topic } from "./arcadeQuestions";

// ══════════════════════════════════════════════════
// C — OUTPUT GUESS (💻)
// ══════════════════════════════════════════════════
export const C_OUTPUT_TOPICS: Topic[] = [
  { id:"c-out-basics", name:"Output: Basics", icon:"💻", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nint x = 5;\nprintf(\"%d\", x++);",options:["5","6","4","Error"],answer:"5",explanation:"x++ is post-increment. Current value (5) is printed first, then x becomes 6."},
    {id:2,question:"What is the output?\n\nint x = 5;\nprintf(\"%d\", ++x);",options:["5","6","4","Error"],answer:"6",explanation:"++x is pre-increment. x becomes 6 first, then 6 is printed."},
    {id:3,question:"What is the output?\n\nprintf(\"%d\", 10 / 3);",options:["3.33","3","4","3.0"],answer:"3",explanation:"Integer division in C discards decimal — 10/3 = 3."},
    {id:4,question:"What is the output?\n\nprintf(\"%d\", 10 % 3);",options:["3","1","0","3.33"],answer:"1",explanation:"% is modulo operator. 10 = 3×3 + 1, so 10 % 3 = 1."},
    {id:5,question:"What is the output?\n\nint a = 5, b = 3;\nprintf(\"%d\", a > b ? a : b);",options:["5","3","1","Error"],answer:"5",explanation:"a > b is true (5 > 3), so ternary returns a = 5."},
    {id:6,question:"What is the output?\n\nprintf(\"%d\", sizeof(int));",options:["1","2","4","8"],answer:"4",explanation:"sizeof(int) is typically 4 bytes on most 32/64-bit systems."},
    {id:7,question:"What is the output?\n\nint x = 5;\nx += 3;\nprintf(\"%d\", x);",options:["5","3","8","Error"],answer:"8",explanation:"x += 3 is x = x + 3 = 8."},
    {id:8,question:"What is the output?\n\nprintf(\"%d\", 2 << 3);",options:["6","8","16","5"],answer:"16",explanation:"Left shift by 3: 2 * 2³ = 2 * 8 = 16."},
    {id:9,question:"What is the output?\n\nint x = 7;\nprintf(\"%d\", x & 3);",options:["7","3","4","1"],answer:"3",explanation:"7 = 0111, 3 = 0011. AND = 0011 = 3."},
    {id:10,question:"What is the output?\n\nint a = 10, b = 3;\nprintf(\"%.2f\", (float)a / b);",options:["3","3.33","3.00","Error"],answer:"3.33",explanation:"(float)a casts a to float before division. 10.0/3 = 3.333..., %.2f prints 2 decimal places = 3.33."},
  ]},
  { id:"c-out-pointers", name:"Output: Pointers", icon:"💻", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nint x = 10;\nint *p = &x;\nprintf(\"%d\", *p);",options:["Address of x","10","0","Error"],answer:"10",explanation:"*p dereferences pointer p — gives value at address stored in p = 10."},
    {id:2,question:"What is the output?\n\nint x = 5;\nint *p = &x;\n*p = 20;\nprintf(\"%d\", x);",options:["5","20","0","Error"],answer:"20",explanation:"*p = 20 changes value at address of x. x and *p refer to same memory location."},
    {id:3,question:"What is the output?\n\nint arr[] = {10, 20, 30};\nint *p = arr;\nprintf(\"%d\", *(p+1));",options:["10","20","30","Error"],answer:"20",explanation:"p points to arr[0]. p+1 points to arr[1]. *(p+1) dereferences to get arr[1] = 20."},
    {id:4,question:"What is the output?\n\nint arr[] = {1, 2, 3};\nprintf(\"%d\", arr[1]);",options:["1","2","3","Error"],answer:"2",explanation:"arr[1] accesses second element (0-indexed). arr[1] = 2."},
    {id:5,question:"What is the output?\n\nint x = 10;\nint *p = &x;\nprintf(\"%d\", p == &x);",options:["0","1","Address","Error"],answer:"1",explanation:"p stores address of x. p == &x compares addresses — they are equal so result is 1 (true)."},
    {id:6,question:"What is the output?\n\nint arr[] = {5, 10, 15};\nint *p = arr;\np++;\nprintf(\"%d\", *p);",options:["5","10","15","Error"],answer:"10",explanation:"p starts at arr[0]. p++ moves to arr[1]. *p = arr[1] = 10."},
    {id:7,question:"What is the output?\n\nchar str[] = \"Hello\";\nprintf(\"%d\", strlen(str));",options:["4","5","6","Error"],answer:"5",explanation:"strlen counts characters excluding null terminator. 'Hello' has 5 characters."},
    {id:8,question:"What is the output?\n\nint a = 5, b = 10;\nint *p = &a;\np = &b;\nprintf(\"%d\", *p);",options:["5","10","Address","Error"],answer:"10",explanation:"p first points to a, then reassigned to point to b. *p now dereferences b = 10."},
    {id:9,question:"What is the output?\n\nint arr[] = {1, 2, 3, 4, 5};\nprintf(\"%d\", *(arr + 4));",options:["1","4","5","Error"],answer:"5",explanation:"arr+4 is pointer to arr[4]. *(arr+4) = arr[4] = 5."},
    {id:10,question:"What is the output?\n\nvoid add(int *a, int b) {\n    *a += b;\n}\nint x = 5;\nadd(&x, 3);\nprintf(\"%d\", x);",options:["5","3","8","Error"],answer:"8",explanation:"Passing &x allows function to modify x via pointer. *a += b means x = x + 3 = 8."},
  ]},
  { id:"c-out-arrays", name:"Output: Arrays & Strings", icon:"💻", difficulty:"Easy", category:"DSA", questions:[
    {id:1,question:"What is the output?\n\nint arr[5] = {0};\nprintf(\"%d\", arr[3]);",options:["Garbage","0","3","Error"],answer:"0",explanation:"int arr[5] = {0} initializes all elements to 0. arr[3] = 0."},
    {id:2,question:"What is the output?\n\nint arr[] = {1,2,3,4,5};\nprintf(\"%d\", sizeof(arr)/sizeof(arr[0]));",options:["5","4","20","Error"],answer:"5",explanation:"sizeof(arr) = 20 bytes (5 ints × 4). sizeof(arr[0]) = 4 bytes. 20/4 = 5 elements."},
    {id:3,question:"What is the output?\n\nchar s[] = \"Hello\";\ns[0] = 'h';\nprintf(\"%s\", s);",options:["Hello","hello","Error","hHello"],answer:"hello",explanation:"s[0] = 'h' changes first character. s is modifiable char array. printf(\"%s\") prints 'hello'."},
    {id:4,question:"What is the output?\n\nint a[] = {1,2,3};\nint b[] = {1,2,3};\nprintf(\"%d\", a == b);",options:["1","0","Error","True"],answer:"0",explanation:"a and b are different arrays at different addresses. a == b compares pointers (addresses) — not equal, so 0."},
    {id:5,question:"What is the output?\n\nchar s1[] = \"abc\";\nchar s2[] = \"abc\";\nprintf(\"%d\", strcmp(s1, s2));",options:["1","-1","0","Error"],answer:"0",explanation:"strcmp returns 0 when strings are equal. s1 and s2 have same content 'abc'."},
    {id:6,question:"What is the output?\n\nint arr[3][2] = {{1,2},{3,4},{5,6}};\nprintf(\"%d\", arr[1][1]);",options:["1","3","4","2"],answer:"4",explanation:"arr[1][1] = row 1, column 1 = 4."},
    {id:7,question:"What is the output?\n\nchar s[] = \"Hello World\";\nprintf(\"%d\", strlen(s));",options:["10","11","12","Error"],answer:"11",explanation:"'Hello World' has 11 characters (including space). strlen excludes null terminator."},
    {id:8,question:"What is the output?\n\nint arr[] = {10,20,30};\nint *p = arr;\nprintf(\"%d %d\", *p, *(p+2));",options:["10 20","10 30","20 30","Error"],answer:"10 30",explanation:"*p = arr[0] = 10. *(p+2) = arr[2] = 30."},
    {id:9,question:"What is the output?\n\nchar s[10];\nstrcpy(s, \"Hi\");\nprintf(\"%d\", strlen(s));",options:["10","2","3","Error"],answer:"2",explanation:"strcpy copies 'Hi' into s. strlen('Hi') = 2 (excludes null terminator)."},
    {id:10,question:"What is the output?\n\nint arr[] = {5,4,3,2,1};\nfor(int i=0; i<5; i++)\n    printf(\"%d \", arr[i]);\n",options:["1 2 3 4 5","5 4 3 2 1","5 4 3 2 1 ","Error"],answer:"5 4 3 2 1 ",explanation:"Loop prints each element followed by space — original order preserved: 5 4 3 2 1 (with trailing space)."},
  ]},
  { id:"c-out-functions", name:"Output: Functions & Recursion", icon:"💻", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nint fact(int n) {\n    if(n <= 1) return 1;\n    return n * fact(n-1);\n}\nprintf(\"%d\", fact(5));",options:["25","120","60","Error"],answer:"120",explanation:"5! = 5×4×3×2×1 = 120. Recursive factorial."},
    {id:2,question:"What is the output?\n\nvoid swap(int a, int b) {\n    int t = a; a = b; b = t;\n}\nint x=5, y=10;\nswap(x, y);\nprintf(\"%d %d\", x, y);",options:["10 5","5 10","0 0","Error"],answer:"5 10",explanation:"C passes by value. swap() modifies local copies, not original x and y. x and y unchanged."},
    {id:3,question:"What is the output?\n\nint fib(int n) {\n    if(n<=1) return n;\n    return fib(n-1) + fib(n-2);\n}\nprintf(\"%d\", fib(6));",options:["8","13","6","5"],answer:"8",explanation:"fib(6) = fib(5)+fib(4) = 5+3 = 8. Fibonacci sequence: 0,1,1,2,3,5,8..."},
    {id:4,question:"What is the output?\n\nint counter = 0;\nvoid increment() {\n    counter++;\n}\nincrement();\nincrement();\nprintf(\"%d\", counter);",options:["0","1","2","Error"],answer:"2",explanation:"counter is global variable — persists across function calls. Two increments = 2."},
    {id:5,question:"What is the output?\n\nvoid foo(int *x) {\n    *x *= 2;\n}\nint n = 5;\nfoo(&n);\nprintf(\"%d\", n);",options:["5","10","2","Error"],answer:"10",explanation:"Pointer passed — *x *= 2 modifies original n. n = 5 * 2 = 10."},
    {id:6,question:"What is the output?\n\nint add(int a, int b) { return a + b; }\nint mul(int a, int b) { return a * b; }\nint (*op)(int,int) = add;\nprintf(\"%d\", op(3, 4));",options:["7","12","Error","3"],answer:"7",explanation:"op is function pointer pointing to add. op(3,4) calls add(3,4) = 7."},
    {id:7,question:"What is the output?\n\nstatic int count = 0;\nvoid increment() {\n    count++;\n}\nincrement();\nincrement();\nincrement();\nprintf(\"%d\", count);",options:["0","1","3","Error"],answer:"3",explanation:"Static local variable persists between function calls. Three increments = count 3."},
    {id:8,question:"What is the output?\n\nint power(int base, int exp) {\n    if(exp == 0) return 1;\n    return base * power(base, exp-1);\n}\nprintf(\"%d\", power(2, 8));",options:["16","64","256","128"],answer:"256",explanation:"2^8 = 256. Recursive power: 2 * 2 * ... * 2 (8 times) = 256."},
    {id:9,question:"What is the output?\n\nvoid print_reverse(int n) {\n    if(n == 0) return;\n    printf(\"%d \", n % 10);\n    print_reverse(n / 10);\n}\nprint_reverse(123);",options:["123","321","1 2 3","3 2 1 "],answer:"3 2 1 ",explanation:"n%10 gets last digit. First call: 3, second: 2, third: 1. Prints digits in reverse order."},
    {id:10,question:"What is the output?\n\nint gcd(int a, int b) {\n    if(b == 0) return a;\n    return gcd(b, a % b);\n}\nprintf(\"%d\", gcd(48, 18));",options:["3","6","9","2"],answer:"6",explanation:"Euclidean algorithm: gcd(48,18)→gcd(18,12)→gcd(12,6)→gcd(6,0)=6."},
  ]},
  { id:"c-out-memory", name:"Output: Memory & Structs", icon:"💻", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nstruct Point { int x; int y; };\nstruct Point p = {3, 4};\nprintf(\"%d %d\", p.x, p.y);",options:["0 0","3 4","Error","3,4"],answer:"3 4",explanation:"Struct initialized with {3,4}. p.x = 3, p.y = 4. Accessed with dot operator."},
    {id:2,question:"What is the output?\n\nstruct Point { int x; int y; };\nstruct Point p = {1, 2};\nstruct Point *ptr = &p;\nprintf(\"%d\", ptr->x);",options:["1","2","Error","Address"],answer:"1",explanation:"ptr->x is arrow operator for pointer to struct. Equivalent to (*ptr).x = p.x = 1."},
    {id:3,question:"What is the output?\n\ntypedef struct {\n    char name[20];\n    int age;\n} Person;\nPerson p = {\"Ali\", 25};\nprintf(\"%s %d\", p.name, p.age);",options:["Error","Ali 25","name age","0"],answer:"Ali 25",explanation:"typedef creates Person alias. p initialized with {\"Ali\", 25}. printf prints name and age."},
    {id:4,question:"What is the output?\n\nint *p = (int*)malloc(sizeof(int));\n*p = 42;\nprintf(\"%d\", *p);\nfree(p);",options:["Error","42","0","Garbage"],answer:"42",explanation:"malloc allocates int-sized memory. *p = 42 stores value. *p prints 42. free() releases memory."},
    {id:5,question:"What is the output?\n\nint arr[] = {1,2,3,4,5};\nint *p = arr + 2;\nprintf(\"%d\", *(p-1));",options:["1","2","3","4"],answer:"2",explanation:"p = arr+2 points to arr[2]=3. p-1 points to arr[1]=2. *(p-1) = 2."},
    {id:6,question:"What is the output?\n\nunion Data {\n    int i;\n    float f;\n};\nunion Data d;\nd.i = 10;\nprintf(\"%d\", d.i);",options:["10","Error","0","Garbage"],answer:"10",explanation:"Union stores one value at a time sharing memory. d.i = 10 sets integer. d.i = 10."},
    {id:7,question:"What is the output?\n\nstruct Node {\n    int data;\n    struct Node *next;\n};\nstruct Node n = {5, NULL};\nprintf(\"%d\", n.data);",options:["NULL","5","Error","0"],answer:"5",explanation:"Self-referential struct for linked list. n.data = 5. n.next = NULL (end of list)."},
    {id:8,question:"What is the output?\n\nint x = 300;\nchar *p = (char*)&x;\nprintf(\"%d\", *p);",options:["300","44","0","Error"],answer:"44",explanation:"300 in binary: 0x12C. Little-endian stores LSB first = 0x2C = 44. Char pointer reads first byte."},
    {id:9,question:"What is the output?\n\nstruct Point {\n    int x, y;\n};\nstruct Point arr[2] = {{1,2},{3,4}};\nprintf(\"%d\", arr[1].y);",options:["1","2","3","4"],answer:"4",explanation:"arr[1] is second struct {3,4}. arr[1].y = 4."},
    {id:10,question:"What is the output?\n\nint arr[] = {10,20,30};\nint *p = arr;\nprintf(\"%d\", 2[p]);",options:["10","20","30","Error"],answer:"30",explanation:"2[p] is same as p[2] = *(p+2) = arr[2] = 30. Array indexing is commutative in C."},
  ]},
];

// ══════════════════════════════════════════════════
// C — BUG FINDER (🐛)
// ══════════════════════════════════════════════════
export const C_BUG_TOPICS: Topic[] = [
  { id:"c-bug-basics", name:"Bug: Basics", icon:"🐛", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\n#include <stdio.h>\nint main() {\n    int x = 5\n    printf(\"%d\", x);\n    return 0;\n}",options:["Wrong format specifier","Missing semicolon after int x = 5","Missing return","Wrong include"],answer:"Missing semicolon after int x = 5",explanation:"Every statement in C must end with semicolon. int x = 5; is required — SyntaxError without it."},
    {id:2,question:"Find the bug:\n\nint x = 5;\nif(x = 5) {\n    printf(\"Five\");\n}",options:["Missing braces","= should be == in condition","Wrong printf","Wrong variable"],answer:"= should be == in condition",explanation:"= is assignment (always true/non-zero), == is comparison. if(x == 5) is the correct comparison."},
    {id:3,question:"Find the bug:\n\nint arr[3] = {1, 2, 3};\nprintf(\"%d\", arr[3]);",options:["Wrong format","Undefined behavior — arr[3] is out of bounds (valid: 0,1,2)","Wrong declaration","printf error"],answer:"Undefined behavior — arr[3] is out of bounds (valid: 0,1,2)",explanation:"arr has indices 0,1,2. arr[3] is out of bounds — undefined behavior in C (no runtime check)."},
    {id:4,question:"Find the bug:\n\nint main() {\n    int *p;\n    *p = 10;\n    printf(\"%d\", *p);\n    return 0;\n}",options:["Wrong format","Uninitialized pointer — segmentation fault","Missing malloc","Wrong declaration"],answer:"Uninitialized pointer — segmentation fault",explanation:"p is declared but not initialized — contains garbage address. Dereferencing causes undefined behavior/segfault."},
    {id:5,question:"Find the bug:\n\nchar str[5];\nstrcpy(str, \"Hello World\");\nprintf(\"%s\", str);",options:["Wrong function","Buffer overflow — 'Hello World' is 12 bytes but str only holds 5","Wrong format","Missing null"],answer:"Buffer overflow — 'Hello World' is 12 bytes but str only holds 5",explanation:"'Hello World' + null terminator = 12 bytes. str can only hold 5 bytes — buffer overflow, undefined behavior."},
    {id:6,question:"Find the bug:\n\nint x = 5;\nprintf(\"%f\", x);",options:["No bug","Wrong format specifier — %d for int, not %f","Missing decimal","Wrong variable"],answer:"Wrong format specifier — %d for int, not %f",explanation:"%f is for float/double. For int use %d. Mismatch causes undefined behavior — garbage output."},
    {id:7,question:"Find the bug:\n\nint main() {\n    int x = 1000000;\n    int y = 1000000;\n    int result = x * y;\n    printf(\"%d\", result);\n    return 0;\n}",options:["No bug","Integer overflow — result exceeds INT_MAX (~2.1 billion)","Wrong operator","Wrong type"],answer:"Integer overflow — result exceeds INT_MAX (~2.1 billion)",explanation:"1000000 * 1000000 = 10^12 which exceeds INT_MAX (2,147,483,647). Use long long and %lld instead."},
    {id:8,question:"Find the bug:\n\nvoid foo() {\n    int arr[5];\n    for(int i=0; i<=5; i++)\n        arr[i] = i;\n}",options:["Wrong loop start","i<=5 causes out-of-bounds — should be i<5","Wrong array","Missing return"],answer:"i<=5 causes out-of-bounds — should be i<5",explanation:"arr has indices 0-4. i<=5 means i goes to 5 — arr[5] is out of bounds. Use i<5."},
    {id:9,question:"Find the bug:\n\nint divide(int a, int b) {\n    return a / b;\n}\nprintf(\"%d\", divide(10, 0));",options:["Wrong return","Division by zero — undefined behavior","Wrong params","Wrong format"],answer:"Division by zero — undefined behavior",explanation:"Dividing integer by zero is undefined behavior in C — typically crashes or gives garbage. Check b != 0 first."},
    {id:10,question:"Find the bug:\n\n#include <stdio.h>\nint main() {\n    printf(\"Hello World\")\n    return 0;\n}",options:["Wrong include","Missing semicolon after printf statement","Wrong string","Missing return"],answer:"Missing semicolon after printf statement",explanation:"printf(\"Hello World\") needs semicolon at end. Every statement in C must end with ;"},
  ]},
  { id:"c-bug-pointers", name:"Bug: Pointers", icon:"🐛", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\nint *foo() {\n    int x = 10;\n    return &x;\n}\nint *p = foo();\nprintf(\"%d\", *p);",options:["No bug","Dangling pointer — x is local, destroyed after foo() returns","Wrong return type","Missing malloc"],answer:"Dangling pointer — x is local, destroyed after foo() returns",explanation:"Returning address of local variable — x is on stack, destroyed when foo() returns. *p is dangling pointer — undefined behavior."},
    {id:2,question:"Find the bug:\n\nint *p = (int*)malloc(sizeof(int));\n*p = 42;\nfree(p);\nprintf(\"%d\", *p);",options:["No bug","Use after free — p points to freed memory","Wrong malloc","Wrong printf"],answer:"Use after free — p points to freed memory",explanation:"After free(p), p is dangling pointer. Accessing *p is undefined behavior — p = NULL after free is best practice."},
    {id:3,question:"Find the bug:\n\nint *p = (int*)malloc(10 * sizeof(int));\nfor(int i=0; i<=10; i++)\n    p[i] = i;\nfree(p);",options:["Missing return","i<=10 — heap buffer overflow (valid indices 0-9)","Wrong malloc","Wrong free"],answer:"i<=10 — heap buffer overflow (valid indices 0-9)",explanation:"malloc allocates 10 ints (indices 0-9). i<=10 writes to p[10] — one past end — heap overflow."},
    {id:4,question:"Find the bug:\n\nint *p = (int*)malloc(sizeof(int));\nif(p != NULL) {\n    *p = 100;\n}\n// forgot free\nreturn 0;",options:["Wrong malloc","Memory leak — free(p) never called","Wrong condition","NULL check wrong"],answer:"Memory leak — free(p) never called",explanation:"malloc'd memory never freed — memory leak. Always pair malloc with free. Use valgrind to detect leaks."},
    {id:5,question:"Find the bug:\n\nvoid swap(int *a, int *b) {\n    int *temp = a;\n    a = b;\n    b = temp;\n}\nint x=5, y=10;\nswap(&x, &y);\nprintf(\"%d %d\", x, y);",options:["No bug","Swaps pointers not values — should swap *a and *b","Wrong parameters","Missing temp"],answer:"Swaps pointers not values — should swap *a and *b",explanation:"Swapping pointer variables (a,b) doesn't affect original. Should be: int temp=*a; *a=*b; *b=temp;"},
    {id:6,question:"Find the bug:\n\nchar *str = \"Hello\";\nstr[0] = 'h';\nprintf(\"%s\", str);",options:["No bug","Undefined behavior — string literals are read-only","Wrong pointer","Wrong char"],answer:"Undefined behavior — string literals are read-only",explanation:"char *str = \"Hello\" points to read-only string literal. Modifying causes undefined behavior. Use char str[] = \"Hello\" instead."},
    {id:7,question:"Find the bug:\n\nint *p;\nint arr[] = {1,2,3};\np = arr;\nfree(p);",options:["No bug","Cannot free stack-allocated array — only heap memory (malloc)","Wrong pointer","Wrong free"],answer:"Cannot free stack-allocated array — only heap memory (malloc)",explanation:"arr is stack-allocated — free() should only be called on heap memory from malloc/calloc/realloc. Calling free on stack = undefined behavior."},
    {id:8,question:"Find the bug:\n\nint **matrix = malloc(3 * sizeof(int*));\nfor(int i=0; i<3; i++)\n    matrix[i] = malloc(3 * sizeof(int));\nmatrix[0][0] = 1;\nfree(matrix);",options:["No bug","Memory leak — inner arrays (matrix[i]) not freed before matrix","Wrong malloc","Wrong access"],answer:"Memory leak — inner arrays (matrix[i]) not freed before matrix",explanation:"Each matrix[i] must be freed before freeing matrix. Reverse order: for(i) free(matrix[i]); then free(matrix)."},
    {id:9,question:"Find the bug:\n\nint arr[] = {1,2,3,4,5};\nint *p = arr;\nwhile(*p != 0)\n    printf(\"%d \", *p++);",options:["No bug","Undefined behavior — no 0 terminator in array, reads past end","Wrong loop","Wrong format"],answer:"Undefined behavior — no 0 terminator in array, reads past end",explanation:"Array has no 0 terminator. While loop reads beyond array bounds looking for 0 — undefined behavior."},
    {id:10,question:"Find the bug:\n\nint x = 5;\nint *p = &x;\nint *q = &x;\n*p = 10;\nprintf(\"%d\", *q);",options:["No bug — prints 10","Two pointers to same variable — but this is actually valid","Segfault","Error"],answer:"No bug — prints 10",explanation:"Multiple pointers to same variable is valid in C. *p = 10 changes x. *q also sees x = 10. Output: 10."},
  ]},
  { id:"c-bug-memory", name:"Bug: Memory & Structs", icon:"🐛", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\nstruct Node {\n    int data;\n    struct Node *next;\n};\nstruct Node *n = malloc(sizeof(Node));",options:["Wrong struct","sizeof(Node) should be sizeof(struct Node) in C","malloc error","Missing typedef"],answer:"sizeof(Node) should be sizeof(struct Node) in C",explanation:"In C (not C++), struct tag requires 'struct' keyword. sizeof(struct Node) is correct unless typedef used."},
    {id:2,question:"Find the bug:\n\ntypedef struct {\n    char *name;\n    int age;\n} Person;\n\nPerson p;\np.name = \"Ali\";\np.age = 25;\nfree(p.name);",options:["No bug","Cannot free string literal — not heap allocated","Wrong typedef","Missing malloc"],answer:"Cannot free string literal — not heap allocated",explanation:"p.name points to string literal (read-only). Calling free() on it is undefined behavior. Only free malloc'd memory."},
    {id:3,question:"Find the bug:\n\nstruct Point { int x; int y; };\nstruct Point *p;\np->x = 5;\np->y = 10;",options:["Wrong arrow operator","Uninitialized pointer p — segmentation fault","Wrong struct","Missing printf"],answer:"Uninitialized pointer p — segmentation fault",explanation:"p is pointer but not allocated. p->x dereferences garbage address — segfault. Use malloc or point to valid struct."},
    {id:4,question:"Find the bug:\n\nchar *create_string() {\n    char buf[50];\n    sprintf(buf, \"Hello World\");\n    return buf;\n}\nchar *s = create_string();\nprintf(\"%s\", s);",options:["Wrong sprintf","Returning address of local buffer — dangling pointer","Wrong format","Missing malloc"],answer:"Returning address of local buffer — dangling pointer",explanation:"buf is on stack — destroyed when function returns. Return malloc'd string or pass buffer as parameter."},
    {id:5,question:"Find the bug:\n\nint *arr = calloc(5, sizeof(int));\narr[0] = 1; arr[1] = 2;\nrealloc(arr, 10 * sizeof(int));\nprintf(\"%d\", arr[0]);",options:["calloc error","realloc result not saved — arr may be invalid if reallocated","Wrong size","No bug"],answer:"realloc result not saved — arr may be invalid if reallocated",explanation:"realloc may move memory. Must save return: int *temp = realloc(arr, ...); if(temp) arr = temp; Not saving = possible dangling pointer."},
    {id:6,question:"Find the bug:\n\n#define MAX 5\nint arr[MAX];\nfor(int i=1; i<=MAX; i++)\n    arr[i] = i * 2;",options:["Wrong macro","Off-by-one — arr[MAX] i.e arr[5] is out of bounds (valid: 0-4)","Wrong loop","Wrong assignment"],answer:"Off-by-one — arr[MAX] i.e arr[5] is out of bounds (valid: 0-4)",explanation:"Array indices 0 to MAX-1 (0-4). i<=MAX goes to i=5 = arr[5] — out of bounds. Use i<MAX and i starts from 0."},
    {id:7,question:"Find the bug:\n\nstruct Stack {\n    int data[100];\n    int top;\n};\nvoid push(struct Stack s, int val) {\n    s.data[++s.top] = val;\n}\nstruct Stack st = {.top = -1};\npush(st, 10);\nprintf(\"%d\", st.top);",options:["Wrong struct","-1 — struct passed by value, changes not reflected","Wrong push","Missing return"],answer:"-1 — struct passed by value, changes not reflected",explanation:"C passes struct by value. push() modifies local copy. st.top remains -1. Pass pointer: void push(struct Stack *s, int val)."},
    {id:8,question:"Find the bug:\n\nint *p = malloc(sizeof(int) * 5);\nfor(int i=0; i<5; i++)\n    p[i] = i;\nfree(p);\nfree(p);",options:["Wrong loop","Double free — undefined behavior, heap corruption","Wrong malloc","Loop error"],answer:"Double free — undefined behavior, heap corruption",explanation:"Calling free() twice on same pointer is undefined behavior — can corrupt heap. Set p = NULL after first free."},
    {id:9,question:"Find the bug:\n\nvoid process(int *arr, int size) {\n    int local[size];\n    memcpy(local, arr, size);\n    // process local\n}",options:["VLA wrong","memcpy size wrong — should be size * sizeof(int)","No bug","Wrong parameter"],answer:"memcpy size wrong — should be size * sizeof(int)",explanation:"memcpy copies bytes. size is element count, not bytes. Should be memcpy(local, arr, size * sizeof(int))."},
    {id:10,question:"Find the bug:\n\nstruct Node *head = NULL;\nstruct Node *curr = head;\nwhile(curr != NULL) {\n    printf(\"%d\", curr->data);\n    curr = curr->next;\n}\nprintf(\"%d\", curr->data);",options:["Wrong loop","curr is NULL after loop — dereferencing NULL pointer","No bug","Wrong printf"],answer:"curr is NULL after loop — dereferencing NULL pointer",explanation:"After while loop, curr == NULL. curr->data dereferences NULL — segfault. printf after loop should not use curr."},
  ]},
  { id:"c-bug-ds", name:"Bug: Data Structures", icon:"🐛", difficulty:"Hard", category:"DSA", questions:[
    {id:1,question:"Find the bug:\n\nstruct Node {\n    int data;\n    struct Node *next;\n};\nvoid append(struct Node *head, int val) {\n    struct Node *new = malloc(sizeof(struct Node));\n    new->data = val;\n    new->next = NULL;\n    while(head->next != NULL)\n        head = head->next;\n    head->next = new;\n}\nstruct Node *head = NULL;\nappend(head, 5);",options:["Wrong malloc","NULL head causes crash — head->next on NULL pointer","Wrong loop","Missing return"],answer:"NULL head causes crash — head->next on NULL pointer",explanation:"When head is NULL, head->next causes segfault. Need to handle empty list case and use double pointer to update head."},
    {id:2,question:"Find the bug:\n\nint stack[100];\nint top = 0;\nvoid push(int x) { stack[top++] = x; }\nint pop() { return stack[top--]; }\n\npush(1); push(2);\nprintf(\"%d\", pop());",options:["Wrong push","pop() returns stack[top] before decrement — wrong element","Stack size","top initialization"],answer:"pop() returns stack[top] before decrement — wrong element",explanation:"top=2 after two pushes. pop() returns stack[2] (empty slot) then decrements. Should be return stack[--top]; (pre-decrement)."},
    {id:3,question:"Find the bug:\n\nint queue[100];\nint front=0, rear=0;\nvoid enqueue(int x) { queue[rear++] = x; }\nint dequeue() { return queue[front++]; }\n\nenqueue(1); enqueue(2);\nprintf(\"%d\", dequeue());",options:["No bug — prints 1","front and rear initialization should both be -1","Wrong array","rear issue"],answer:"No bug — prints 1",explanation:"front=0 means first element is at queue[0]. enqueue stores at rear, dequeue reads from front. dequeue() returns queue[0]=1. Correct."},
    {id:4,question:"Find the bug:\n\nstruct Node {\n    int data;\n    struct Node *next;\n};\nvoid delete_node(struct Node *prev, struct Node *del) {\n    prev->next = del->next;\n    free(del);\n}\n// After this del is still accessible in calling code",options:["No bug","Dangling pointer — caller may still use del after free","Wrong logic","Missing NULL"],answer:"Dangling pointer — caller may still use del after free",explanation:"del is freed but caller might access it. Set del = NULL after free in calling code, or use double pointer to nullify."},
    {id:5,question:"Find the bug:\n\nint binary_search(int arr[], int n, int target) {\n    int lo=0, hi=n;\n    while(lo < hi) {\n        int mid = lo + (hi-lo)/2;\n        if(arr[mid] == target) return mid;\n        else if(arr[mid] < target) lo = mid+1;\n        else hi = mid;\n    }\n    return -1;\n}",options:["No bug","hi=n should be hi=n-1 — arr[n] is out of bounds","Wrong mid","Wrong return"],answer:"hi=n should be hi=n-1 — arr[n] is out of bounds",explanation:"Valid indices are 0 to n-1. hi=n and accessing arr[mid] when mid can equal n causes out-of-bounds."},
    {id:6,question:"Find the bug:\n\nvoid bubble_sort(int arr[], int n) {\n    for(int i=0; i<n; i++)\n        for(int j=0; j<n; j++)\n            if(arr[j] > arr[j+1]) {\n                int t=arr[j];\n                arr[j]=arr[j+1];\n                arr[j+1]=t;\n            }\n}",options:["Wrong outer loop","Inner loop j<n causes arr[j+1] out of bounds — should be j<n-1-i","Wrong swap","Wrong condition"],answer:"Inner loop j<n causes arr[j+1] out of bounds — should be j<n-1-i",explanation:"When j=n-1, arr[j+1]=arr[n] is out of bounds. Inner loop should be j<n-1. Optimized: j<n-1-i."},
    {id:7,question:"Find the bug:\n\nstruct Node* reverse_list(struct Node* head) {\n    struct Node *prev=NULL, *curr=head, *next;\n    while(curr) {\n        next = curr->next;\n        curr->next = prev;\n        curr = next;\n        prev = curr;\n    }\n    return prev;\n}",options:["No bug","Wrong order — prev=curr should come before curr=next","Wrong return","Missing null check"],answer:"Wrong order — prev=curr should come before curr=next",explanation:"curr=next moves curr forward first. Then prev=curr sets prev to new position. Should be: prev=curr; curr=next; — in that order."},
    {id:8,question:"Find the bug:\n\nint hash(int key, int size) {\n    return key % size;\n}\n// Table size = 0 passed by mistake\nprintf(\"%d\", hash(10, 0));",options:["Wrong function","Division by zero — size=0 causes undefined behavior","Wrong format","No bug"],answer:"Division by zero — size=0 causes undefined behavior",explanation:"key % 0 is undefined behavior (same as division by zero). Always validate size > 0 before hashing."},
    {id:9,question:"Find the bug:\n\nstruct Node* create(int val) {\n    struct Node *n = malloc(sizeof(struct Node));\n    n->data = val;\n    n->next = NULL;\n    return n;\n}\n// malloc can return NULL if out of memory\nstruct Node *n = create(5);\nprintf(\"%d\", n->data);",options:["No bug","Missing NULL check — if malloc fails, n->data crashes","Wrong malloc","Wrong return"],answer:"Missing NULL check — if malloc fails, n->data crashes",explanation:"malloc can return NULL if memory allocation fails. Always check: if(n == NULL) handle error; before accessing n->data."},
    {id:10,question:"Find the bug:\n\nvoid inorder(struct Node *root) {\n    inorder(root->left);\n    printf(\"%d \", root->data);\n    inorder(root->right);\n}",options:["Wrong order","Missing base case — infinite recursion when root is NULL","Wrong printf","Wrong traversal"],answer:"Missing base case — infinite recursion when root is NULL",explanation:"No NULL check — when root is NULL (leaf's child), inorder still tries root->left causing segfault. Add: if(root==NULL) return;"},
  ]},
];

// ══════════════════════════════════════════════════
// C — FILL IN THE BLANK (⬜)
// ══════════════════════════════════════════════════
export const C_FILL_TOPICS: Topic[] = [
  { id:"c-fill-basics", name:"Fill: Basics", icon:"⬜", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"Complete to print integer:\n\nint x = 42;\nprintf(\"____%d\", x);",options:["\"%d\"","\"%i\"","\"%f\"","\"%s\""],answer:"\"%d\"",explanation:"%d is format specifier for integer. printf(\"%d\", x) prints integer value."},
    {id:2,question:"Complete the scanf:\n\nint n;\nscanf(\"____%d\", &n);",options:["\"%d\"","\"%i\"","\"%f\"","\"d\""],answer:"\"%d\"",explanation:"scanf(\"%d\", &n) reads integer. & gives address of n for scanf to write to."},
    {id:3,question:"Complete to declare constant:\n\n_____ int MAX = 100;",options:["static","const","final","immutable"],answer:"const",explanation:"const keyword makes variable read-only. const int MAX = 100 cannot be modified after initialization."},
    {id:4,question:"Complete the for loop to print 1 to 5:\n\nfor(int i=1; i_____5; i++)\n    printf(\"%d \", i);",options:["<=","<","==",">="],answer:"<=",explanation:"i<=5 means loop runs while i is 1,2,3,4,5. i<5 would stop at 4."},
    {id:5,question:"Complete the while loop:\n\nint i = 0;\n_____(i < 5) {\n    printf(\"%d \", i++);\n}",options:["for","if","while","do"],answer:"while",explanation:"while(condition) executes block repeatedly as long as condition is true."},
    {id:6,question:"Complete to get string length:\n\nchar s[] = \"Hello\";\nint len = _____(s);\nprintf(\"%d\", len);",options:["length","sizeof","strlen","len"],answer:"strlen",explanation:"strlen() from <string.h> returns length excluding null terminator. sizeof includes null terminator."},
    {id:7,question:"Complete to copy string:\n\nchar src[] = \"Hello\";\nchar dest[10];\n_____(dest, src);",options:["strcopy","memcpy","strcpy","strdup"],answer:"strcpy",explanation:"strcpy(dest, src) copies string including null terminator. From <string.h>."},
    {id:8,question:"Complete the switch:\n\nint x = 2;\nswitch(x) {\n    case 1: printf(\"One\"); _____;\n    case 2: printf(\"Two\"); _____;\n    default: printf(\"Other\");\n}",options:["continue / continue","break / break","return / return","exit / exit"],answer:"break / break",explanation:"break exits switch. Without break, execution falls through to next case."},
    {id:9,question:"Complete to allocate array:\n\nint n = 5;\nint *arr = (int*)_____(n * sizeof(int));",options:["new","alloc","malloc","create"],answer:"malloc",explanation:"malloc(size) allocates heap memory. Cast to int*. Returns NULL if fails. Remember to free()."},
    {id:10,question:"Complete to include standard I/O:\n\n_____<stdio.h>\n\nint main() {\n    printf(\"Hello\");\n    return 0;\n}",options:["import","using","#include","require"],answer:"#include",explanation:"#include<stdio.h> includes Standard Input/Output header — provides printf, scanf, etc."},
  ]},
  { id:"c-fill-pointers", name:"Fill: Pointers", icon:"⬜", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"Complete to declare pointer:\n\nint x = 10;\nint _____ p = &x;\nprintf(\"%d\", *p);",options:["&","*","**","->"],answer:"*",explanation:"int *p declares pointer to int. &x gives address of x. *p dereferences to get value."},
    {id:2,question:"Complete to pass by pointer:\n\nvoid double_it(int _____n) {\n    *n *= 2;\n}\nint x = 5;\ndouble_it(_____x);",options:["* / &","& / *","** / &&","* / *"],answer:"* / &",explanation:"int *n is pointer parameter. &x passes address of x. *n *= 2 modifies original x."},
    {id:3,question:"Complete the dynamic allocation:\n\nint *arr = (int*)malloc(5 * _____(int));\nfor(int i=0; i<5; i++)\n    arr[i] = i;\nfree(arr);",options:["size","length","sizeof","bytes"],answer:"sizeof",explanation:"sizeof(int) returns bytes for one int. 5 * sizeof(int) = 20 bytes for 5 integers."},
    {id:4,question:"Complete to iterate with pointer:\n\nint arr[] = {1,2,3,4,5};\nint *p = arr;\nint *end = arr + 5;\nwhile(p _____ end)\n    printf(\"%d \", *p++);",options:["<","<=","!=","=="],answer:"!=",explanation:"p != end checks if pointer reached one past array. Both < and != work, != is idiomatic for pointer iteration."},
    {id:5,question:"Complete the function pointer:\n\nint add(int a, int b) { return a+b; }\nint (_____ op)(int,int) = add;\nprintf(\"%d\", op(3,4));",options:["&","*","**","->"],answer:"*",explanation:"int (*op)(int,int) declares function pointer. Parentheses required — int *op() would be function returning int*."},
    {id:6,question:"Complete the NULL check:\n\nint *p = malloc(sizeof(int));\nif(p _____ NULL) {\n    printf(\"malloc failed\");\n    return 1;\n}\n*p = 42;",options:["=","!=","==","is"],answer:"==",explanation:"malloc returns NULL on failure. Check if(p == NULL) before using pointer to avoid segfault."},
    {id:7,question:"Complete to swap with pointers:\n\nvoid swap(int *a, int *b) {\n    int temp = _____a;\n    _____a = _____b;\n    _____b = temp;\n}",options:["* / * / * / *","& / & / & / &","** / ** / ** / **","* / & / * / &"],answer:"* / * / * / *",explanation:"*a and *b dereference pointers to access values. temp = *a saves value, *a = *b copies, *b = temp restores."},
    {id:8,question:"Complete the double pointer:\n\nvoid allocate(int _____ptr, int size) {\n    *ptr = (int*)malloc(size * sizeof(int));\n}\nint *arr;\nallocate(_____arr, 5);",options:["** / &","* / &","** / *","& / &"],answer:"** / &",explanation:"int **ptr is pointer to pointer. &arr passes address of arr pointer. *ptr = malloc assigns to arr."},
    {id:9,question:"Complete the const pointer:\n\nconst char _____msg = \"Hello\";\n// msg cannot be changed to point elsewhere\n// but content also read-only here",options:["*","**","* const","const *"],answer:"*",explanation:"const char *msg: msg can point elsewhere but content is read-only. char * const msg: cannot point elsewhere but content modifiable."},
    {id:10,question:"Complete pointer arithmetic:\n\nint arr[] = {10,20,30,40,50};\nint *p = arr + 2;\nprintf(\"%d\", *(_____ - 1));",options:["arr","p","&arr","*p"],answer:"p",explanation:"p points to arr[2]=30. p-1 points to arr[1]=20. *(p-1) = 20."},
  ]},
  { id:"c-fill-ds", name:"Fill: Data Structures", icon:"⬜", difficulty:"Hard", category:"DSA", questions:[
    {id:1,question:"Complete the linked list node:\n\nstruct Node {\n    int data;\n    struct _____ *next;\n};",options:["Node","int","void","List"],answer:"Node",explanation:"Self-referential struct — struct Node contains pointer to another struct Node. This is how linked lists are built."},
    {id:2,question:"Complete linked list insertion at head:\n\nstruct Node* insert_head(struct Node* head, int val) {\n    struct Node *new = malloc(sizeof(struct Node));\n    new->data = val;\n    new->next = _____;\n    return new;\n}",options:["NULL","new","head","malloc"],answer:"head",explanation:"new->next = head links new node to existing list. Return new as new head. This prepends to list."},
    {id:3,question:"Complete the stack push:\n\nvoid push(int *stack, int *top, int val) {\n    stack[_____] = val;\n}\n// top starts at -1",options:["(*top)++","++(*top)","*top","top"],answer:"++(*top)",explanation:"Pre-increment: ++(*top) increments top first (from -1 to 0), then stores at that index. Correct LIFO behavior."},
    {id:4,question:"Complete binary search:\n\nint binary_search(int arr[], int n, int target) {\n    int lo=0, hi=n-1;\n    while(lo<=hi) {\n        int mid = lo + (hi-_____)/2;\n        if(arr[mid]==target) return mid;\n        if(arr[mid]<target) lo=mid+1;\n        else hi=mid-1;\n    }\n    return -1;\n}",options:["lo","hi","mid","0"],answer:"lo",explanation:"mid = lo + (hi-lo)/2 avoids integer overflow compared to (lo+hi)/2. Standard safe mid calculation."},
    {id:5,question:"Complete inorder traversal:\n\nvoid inorder(struct Node *root) {\n    if(root == NULL) _____;\n    inorder(root->left);\n    printf(\"%d \", root->data);\n    inorder(root->right);\n}",options:["break","exit(0)","return","continue"],answer:"return",explanation:"Base case: if(root == NULL) return; stops recursion at leaf nodes. Essential for tree traversal."},
    {id:6,question:"Complete bubble sort:\n\nvoid bubble_sort(int arr[], int n) {\n    for(int i=0; i<n-1; i++)\n        for(int j=0; j<n-1-i; j++)\n            if(arr[j] > arr[j+1]) {\n                int t = arr[j];\n                arr[j] = _____;\n                arr[j+1] = t;\n            }\n}",options:["arr[j]","arr[j+1]","t","0"],answer:"arr[j+1]",explanation:"Swap arr[j] and arr[j+1]: save arr[j] in t, set arr[j]=arr[j+1], set arr[j+1]=t."},
    {id:7,question:"Complete hash function:\n\nint hash(char *key, int size) {\n    int hash = 0;\n    while(*key)\n        hash = (hash * 31 + _____key) % size;\n    return hash;\n}\n",options:["*","&","**","->"],answer:"*",explanation:"*key dereferences character pointer to get current character value. key pointer advances with key++."},
    {id:8,question:"Complete BFS with queue:\n\nvoid bfs(int graph[][MAX], int start, int n) {\n    int visited[MAX] = {0};\n    int queue[MAX];\n    int front=0, rear=0;\n    queue[rear_____] = start;\n    visited[start] = 1;\n    while(front != rear) {\n        int v = queue[front_____];\n        printf(\"%d \", v);\n    }\n}",options:["++ / ++","-- / --","++ / --","+1 / -1"],answer:"++ / ++",explanation:"rear++ enqueues: store then increment rear. front++ dequeues: read then increment front."},
    {id:9,question:"Complete the merge sort merge:\n\nvoid merge(int arr[], int l, int m, int r) {\n    int n1=m-l+1, n2=r-m;\n    int L[n1], R[n2];\n    for(int i=0;i<n1;i++) L[i]=arr[l+i];\n    for(int j=0;j<n2;j++) R[j]=arr[_____];\n    // merge L and R back into arr\n}",options:["m+j","m+1+j","r-j","l+j"],answer:"m+1+j",explanation:"Right subarray starts at m+1. R[j] = arr[m+1+j] copies right half. Left half: arr[l+i]."},
    {id:10,question:"Complete Floyd's cycle detection:\n\nint has_cycle(struct Node *head) {\n    struct Node *slow = head, *fast = head;\n    while(fast != NULL && fast->next != NULL) {\n        slow = slow->_____;\n        fast = fast->next._____;\n        if(slow == fast) return 1;\n    }\n    return 0;\n}",options:["next / next","next / next->next","next->next / next","data / data"],answer:"next / next->next",explanation:"slow moves 1 step: slow=slow->next. fast moves 2 steps: fast=fast->next->next. If they meet, cycle exists."},
  ]},
  { id:"c-fill-memory", name:"Fill: Memory Management", icon:"⬜", difficulty:"Hard", category:"Core", questions:[
    {id:1,question:"Complete malloc with NULL check:\n\nint *arr = (int*)malloc(n * sizeof(int));\nif(arr _____ NULL) {\n    fprintf(stderr, \"malloc failed\");\n    exit(1);\n}",options:["=","!=","==","is"],answer:"==",explanation:"malloc returns NULL on failure. if(arr == NULL) checks failure. Always check before using allocated memory."},
    {id:2,question:"Complete calloc usage:\n\n// Allocate and zero-initialize 10 ints\nint *arr = (int*)_____(10, sizeof(int));",options:["malloc","alloc","calloc","zalloc"],answer:"calloc",explanation:"calloc(count, size) allocates and zero-initializes. calloc(10, sizeof(int)) = 10 zero-initialized ints."},
    {id:3,question:"Complete safe realloc:\n\nint *arr = malloc(5 * sizeof(int));\nint *temp = (int*)_____(arr, 10 * sizeof(int));\nif(temp != NULL)\n    arr = temp;\n// else arr still valid",options:["malloc","calloc","realloc","expand"],answer:"realloc",explanation:"realloc resizes allocation. Save to temp — if it fails, original arr still valid. Never realloc directly into arr."},
    {id:4,question:"Complete to free 2D array:\n\nint **matrix = malloc(rows * sizeof(int*));\nfor(int i=0; i<rows; i++)\n    matrix[i] = malloc(cols * sizeof(int));\n\n// Freeing:\nfor(int i=0; i<rows; i++)\n    _____(matrix[i]);\n_____(matrix);",options:["free / free","delete / delete","release / release","free / delete"],answer:"free / free",explanation:"Free inner arrays first, then outer array. Reverse order of allocation. Both use free()."},
    {id:5,question:"Complete to prevent memory leak:\n\nvoid process() {\n    int *data = malloc(100 * sizeof(int));\n    if(data == NULL) return;\n    // use data\n    _____(data);\n    data = _____;\n}",options:["free / NULL","delete / 0","release / NULL","free / 0"],answer:"free / NULL",explanation:"free(data) releases memory. data = NULL prevents dangling pointer use after free — defensive programming."},
    {id:6,question:"Complete memset usage:\n\nint arr[10];\n_____(arr, 0, sizeof(arr));",options:["memcpy","memset","bzero","fill"],answer:"memset",explanation:"memset(ptr, value, bytes) sets memory to byte value. memset(arr, 0, sizeof(arr)) zeros all elements."},
    {id:7,question:"Complete memcpy:\n\nint src[] = {1,2,3,4,5};\nint dest[5];\n_____(dest, src, 5 * sizeof(int));",options:["strcpy","memcpy","memset","memmove"],answer:"memcpy",explanation:"memcpy(dest, src, bytes) copies bytes. For non-overlapping arrays. Use memmove if arrays might overlap."},
    {id:8,question:"Complete struct copy:\n\ntypedef struct { int x; int y; } Point;\nPoint p1 = {1, 2};\nPoint p2;\n_____ = p1;",options:["*p2","&p2","p2","p2.data"],answer:"p2",explanation:"p2 = p1 copies struct by value in C — all fields copied. Simple assignment works for structs without pointers."},
    {id:9,question:"Complete to use valgrind-friendly code:\n\nchar *str = (char*)malloc(strlen(source) + _____);",options:["0","1","2","sizeof(char)"],answer:"1",explanation:"strlen + 1 for null terminator. strlen returns length without '\\0'. malloc needs space for null terminator."},
    {id:10,question:"Complete the memory pool pattern:\n\n#define POOL_SIZE 1000\nchar pool[POOL_SIZE];\nint pool_offset = 0;\n\nvoid* pool_alloc(int size) {\n    if(pool_offset + size > POOL_SIZE) return NULL;\n    void *ptr = pool + _____;\n    pool_offset += size;\n    return ptr;\n}",options:["pool_offset","size","0","POOL_SIZE"],answer:"pool_offset",explanation:"ptr = pool + pool_offset gives next available position. After returning, advance pool_offset by size."},
  ]},
];