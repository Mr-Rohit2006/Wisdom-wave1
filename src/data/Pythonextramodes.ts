import type { Topic } from "./arcadeQuestions";

// ══════════════════════════════════════════════════
// PYTHON — OUTPUT GUESS (💻)
// ══════════════════════════════════════════════════
export const PYTHON_OUTPUT_TOPICS: Topic[] = [
  { id:"py-out-basics", name:"Output: Basics", icon:"💻", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"What is the output?\n\nx = 5\nprint(x++)",options:["5","6","SyntaxError","None"],answer:"SyntaxError",explanation:"Python does not have ++ operator. Use x += 1 instead."},
    {id:2,question:"What is the output?\n\nprint(10 // 3)",options:["3.33","3","4","3.0"],answer:"3",explanation:"// is floor division in Python — discards decimal and returns integer 3."},
    {id:3,question:"What is the output?\n\nprint(10 % 3)",options:["3","1","0","3.33"],answer:"1",explanation:"% is modulo operator — remainder of 10 / 3 = 1."},
    {id:4,question:"What is the output?\n\nprint(2 ** 3)",options:["6","8","9","23"],answer:"8",explanation:"** is the power operator in Python. 2 ** 3 = 2³ = 8."},
    {id:5,question:"What is the output?\n\nx = 5\nprint(x > 3 and x < 10)",options:["True","False","1","Error"],answer:"True",explanation:"5 > 3 is True and 5 < 10 is True. True and True = True."},
    {id:6,question:"What is the output?\n\nprint(type(5))",options:["int","<class 'int'>","integer","5"],answer:"<class 'int'>",explanation:"type() returns the class of the value. Python prints it as <class 'int'>."},
    {id:7,question:"What is the output?\n\nprint(bool(0), bool(''), bool([]))",options:["False False False","True True True","0 '' []","Error"],answer:"False False False",explanation:"0, empty string '', and empty list [] are all falsy in Python — bool() returns False."},
    {id:8,question:"What is the output?\n\nprint(3 / 2)",options:["1","1.5","2","Error"],answer:"1.5",explanation:"/ always returns float in Python 3. Use // for integer division."},
    {id:9,question:"What is the output?\n\nx = 10\nx += 5\nprint(x)",options:["10","5","15","Error"],answer:"15",explanation:"x += 5 is shorthand for x = x + 5 = 15."},
    {id:10,question:"What is the output?\n\nprint('5' + '3')",options:["8","53","'53'","Error"],answer:"53",explanation:"+ with strings is concatenation — '5' + '3' = '53', not arithmetic 8."},
  ]},
  { id:"py-out-lists", name:"Output: Lists", icon:"💻", difficulty:"Easy", category:"DSA", questions:[
    {id:1,question:"What is the output?\n\narr = [1, 2, 3]\nprint(arr[-1])",options:["1","3","Error","-1"],answer:"3",explanation:"Negative indexing: arr[-1] is the last element = 3."},
    {id:2,question:"What is the output?\n\narr = [1, 2, 3, 4, 5]\nprint(arr[1:3])",options:["[1,2,3]","[2,3]","[2,3,4]","Error"],answer:"[2,3]",explanation:"Slicing arr[1:3] returns elements from index 1 up to (not including) index 3 = [2, 3]."},
    {id:3,question:"What is the output?\n\na = [1, 2, 3]\nb = a\nb[0] = 99\nprint(a[0])",options:["1","99","0","Error"],answer:"99",explanation:"Lists are reference types. b = a means both point to same list. Changing b[0] also changes a[0]."},
    {id:4,question:"What is the output?\n\narr = [3, 1, 2]\narr.sort()\nprint(arr[0])",options:["3","1","2","Error"],answer:"1",explanation:"sort() sorts in-place ascending. After sorting [3,1,2] → [1,2,3]. arr[0] = 1."},
    {id:5,question:"What is the output?\n\narr = [1, 2, 3]\narr.append(4)\nprint(len(arr))",options:["3","4","5","Error"],answer:"4",explanation:"append() adds one element to end. Length becomes 4."},
    {id:6,question:"What is the output?\n\narr = [1, 2, 3, 4]\nprint(arr[::2])",options:["[1,3]","[2,4]","[1,2]","[1,2,3,4]"],answer:"[1,3]",explanation:"arr[::2] starts from 0 with step 2 — picks index 0,2 = [1, 3]."},
    {id:7,question:"What is the output?\n\narr = [1, 2, 3]\nprint(arr * 2)",options:["[2,4,6]","[1,2,3,1,2,3]","6","Error"],answer:"[1,2,3,1,2,3]",explanation:"* on lists repeats the list. [1,2,3] * 2 = [1,2,3,1,2,3]."},
    {id:8,question:"What is the output?\n\narr = [5, 3, 1, 4, 2]\nprint(sorted(arr))",options:["[1,2,3,4,5]","[5,4,3,2,1]","None","Error"],answer:"[1,2,3,4,5]",explanation:"sorted() returns new sorted list without modifying original. arr.sort() modifies in-place."},
    {id:9,question:"What is the output?\n\narr = [1, 2, 3]\nprint(2 in arr)",options:["True","False","2","Error"],answer:"True",explanation:"in operator checks membership. 2 is in [1,2,3] so returns True."},
    {id:10,question:"What is the output?\n\narr = [1, 2, 3]\nx = arr.pop()\nprint(x, arr)",options:["1 [2,3]","3 [1,2]","3 [1,2,3]","Error"],answer:"3 [1,2]",explanation:"pop() removes and returns last element. x=3, arr becomes [1,2]."},
  ]},
  { id:"py-out-strings", name:"Output: Strings", icon:"💻", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"What is the output?\n\ns = 'Hello World'\nprint(s[6:])",options:["Hello","World","Hello World","Error"],answer:"World",explanation:"s[6:] slices from index 6 to end = 'World'."},
    {id:2,question:"What is the output?\n\ns = 'hello'\nprint(s.upper())",options:["hello","HELLO","Hello","Error"],answer:"HELLO",explanation:"upper() converts all characters to uppercase."},
    {id:3,question:"What is the output?\n\nprint('Python'[::-1])",options:["Python","nohtyP","Error","P"],answer:"nohtyP",explanation:"[::-1] reverses the string — step of -1 goes backwards."},
    {id:4,question:"What is the output?\n\ns = '  hello  '\nprint(s.strip())",options:["'  hello  '","hello","'hello'","Error"],answer:"hello",explanation:"strip() removes leading and trailing whitespace."},
    {id:5,question:"What is the output?\n\nname = 'Ali'\nage = 25\nprint(f'{name} is {age}')",options:["name is age","Ali is 25","{name} is {age}","Error"],answer:"Ali is 25",explanation:"f-strings interpolate variables inside {}. f'{name} is {age}' = 'Ali is 25'."},
    {id:6,question:"What is the output?\n\ns = 'hello'\nprint(s.replace('l', 'r'))",options:["herlo","herro","hello","Error"],answer:"herro",explanation:"replace() replaces all occurrences of first arg with second. Both 'l' replaced = 'herro'."},
    {id:7,question:"What is the output?\n\nprint('ha' * 3)",options:["hahaha","ha ha ha","3","Error"],answer:"hahaha",explanation:"String * n repeats the string n times. 'ha' * 3 = 'hahaha'."},
    {id:8,question:"What is the output?\n\ns = 'hello,world,python'\nprint(s.split(','))",options:["['hello world python']","['hello','world','python']","hello world python","Error"],answer:"['hello','world','python']",explanation:"split(',') splits string by comma delimiter into list of strings."},
    {id:9,question:"What is the output?\n\nprint(len('Hello'))",options:["4","5","6","Error"],answer:"5",explanation:"len() returns number of characters. 'Hello' has 5 characters."},
    {id:10,question:"What is the output?\n\nprint('py' in 'python')",options:["True","False","py","Error"],answer:"True",explanation:"in operator checks substring. 'py' is in 'python' so True."},
  ]},
  { id:"py-out-functions", name:"Output: Functions", icon:"💻", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"What is the output?\n\ndef greet(name='World'):\n    return f'Hello {name}'\n\nprint(greet())",options:["Hello","Hello World","Hello name","Error"],answer:"Hello World",explanation:"Default parameter name='World' used when no argument passed."},
    {id:2,question:"What is the output?\n\ndef add(a, b):\n    return a + b\n\nprint(add(b=3, a=2))",options:["5","23","Error","None"],answer:"5",explanation:"Keyword arguments can be passed in any order. a=2, b=3 → 2+3=5."},
    {id:3,question:"What is the output?\n\ndef multiply(*args):\n    result = 1\n    for n in args:\n        result *= n\n    return result\n\nprint(multiply(2, 3, 4))",options:["9","24","234","Error"],answer:"24",explanation:"*args collects all positional arguments into tuple. 2*3*4 = 24."},
    {id:4,question:"What is the output?\n\nx = 10\ndef foo():\n    x = 20\n    print(x)\n\nfoo()\nprint(x)",options:["20 20","10 10","20 10","10 20"],answer:"20 10",explanation:"foo() creates local x=20, prints 20. Global x=10 unchanged, prints 10."},
    {id:5,question:"What is the output?\n\ndef counter():\n    count = 0\n    def inc():\n        nonlocal count\n        count += 1\n        return count\n    return inc\n\nc = counter()\nprint(c(), c())",options:["0 0","1 1","1 2","Error"],answer:"1 2",explanation:"nonlocal allows inner function to modify outer variable. Each call increments count."},
    {id:6,question:"What is the output?\n\nsquare = lambda x: x ** 2\nprint(square(5))",options:["10","25","5","Error"],answer:"25",explanation:"Lambda function: square(5) = 5 ** 2 = 25."},
    {id:7,question:"What is the output?\n\ndef foo(lst):\n    lst.append(4)\n\narr = [1, 2, 3]\nfoo(arr)\nprint(arr)",options:["[1,2,3]","[1,2,3,4]","None","Error"],answer:"[1,2,3,4]",explanation:"Lists are mutable and passed by reference. append inside function modifies the original list."},
    {id:8,question:"What is the output?\n\nprint(list(map(lambda x: x*2, [1,2,3])))",options:["[1,2,3]","[2,4,6]","[1,4,9]","Error"],answer:"[2,4,6]",explanation:"map() applies function to each element. x*2 for [1,2,3] = [2,4,6]. list() converts map object."},
    {id:9,question:"What is the output?\n\nprint(list(filter(lambda x: x%2==0, [1,2,3,4,5])))",options:["[1,3,5]","[2,4]","[1,2,3,4,5]","Error"],answer:"[2,4]",explanation:"filter() keeps elements where function returns True. Even numbers from [1,2,3,4,5] = [2,4]."},
    {id:10,question:"What is the output?\n\ndef foo():\n    pass\n\nprint(foo())",options:["0","None","pass","Error"],answer:"None",explanation:"Function with no return statement (or just pass) returns None implicitly."},
  ]},
  { id:"py-out-oop", name:"Output: OOP", icon:"💻", difficulty:"Medium", category:"OOPs", questions:[
    {id:1,question:"What is the output?\n\nclass Dog:\n    def __init__(self, name):\n        self.name = name\n\nd = Dog('Rex')\nprint(d.name)",options:["Dog","Rex","name","Error"],answer:"Rex",explanation:"__init__ sets self.name = 'Rex'. d.name accesses the instance attribute."},
    {id:2,question:"What is the output?\n\nclass Counter:\n    count = 0\n    def __init__(self):\n        Counter.count += 1\n\nCounter()\nCounter()\nprint(Counter.count)",options:["0","1","2","Error"],answer:"2",explanation:"count is a class variable shared by all instances. Each __init__ increments it. 2 instances = count 2."},
    {id:3,question:"What is the output?\n\nclass Animal:\n    def speak(self):\n        return 'Generic sound'\n\nclass Dog(Animal):\n    def speak(self):\n        return 'Woof'\n\nd = Dog()\nprint(d.speak())",options:["Generic sound","Woof","Dog","Error"],answer:"Woof",explanation:"Dog overrides speak(). Polymorphism — Dog instance calls Dog's version."},
    {id:4,question:"What is the output?\n\nclass A:\n    def __init__(self):\n        self.x = 1\n\nclass B(A):\n    def __init__(self):\n        super().__init__()\n        self.y = 2\n\nb = B()\nprint(b.x, b.y)",options:["Error","1 2","0 0","None None"],answer:"1 2",explanation:"super().__init__() calls A's __init__ setting x=1. Then y=2 is set. b has both x and y."},
    {id:5,question:"What is the output?\n\nclass MyClass:\n    def __str__(self):\n        return 'Hello from MyClass'\n\nobj = MyClass()\nprint(obj)",options:["<MyClass object>","Hello from MyClass","MyClass","Error"],answer:"Hello from MyClass",explanation:"__str__ defines string representation used by print(). Dunder method override."},
    {id:6,question:"What is the output?\n\nclass MyClass:\n    @staticmethod\n    def greet():\n        return 'Hello'\n\nprint(MyClass.greet())",options:["Error","Hello","None","MyClass"],answer:"Hello",explanation:"@staticmethod can be called on class directly without instance. No self parameter needed."},
    {id:7,question:"What is the output?\n\nclass MyClass:\n    def __init__(self):\n        self.__secret = 42\n\nobj = MyClass()\nprint(obj.__secret)",options:["42","None","AttributeError","Error"],answer:"AttributeError",explanation:"Name mangling: __secret becomes _MyClass__secret. Accessing obj.__secret raises AttributeError."},
    {id:8,question:"What is the output?\n\nclass Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    def __add__(self, other):\n        return Point(self.x+other.x, self.y+other.y)\n\np = Point(1,2) + Point(3,4)\nprint(p.x, p.y)",options:["1 2","3 4","4 6","Error"],answer:"4 6",explanation:"__add__ enables + operator. (1+3, 2+4) = Point(4, 6)."},
    {id:9,question:"What is the output?\n\nclass A:\n    pass\n\nclass B(A):\n    pass\n\nb = B()\nprint(isinstance(b, A))",options:["False","True","Error","None"],answer:"True",explanation:"isinstance checks class hierarchy. B inherits from A, so B instance is also an A instance."},
    {id:10,question:"What is the output?\n\nfrom dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: int\n    y: int\n\np = Point(1, 2)\nprint(p)",options:["<Point object>","Point(x=1, y=2)","1 2","Error"],answer:"Point(x=1, y=2)",explanation:"@dataclass auto-generates __repr__. print() uses __repr__ if __str__ not defined."},
  ]},
];

// ══════════════════════════════════════════════════
// PYTHON — BUG FINDER (🐛)
// ══════════════════════════════════════════════════
export const PYTHON_BUG_TOPICS: Topic[] = [
  { id:"py-bug-basics", name:"Bug: Basics", icon:"🐛", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\ndef greet(name)\n    print(f'Hello {name}')\n\ngreet('Ali')",options:["Missing colon after def","Wrong f-string","Missing return","Wrong indentation"],answer:"Missing colon after def",explanation:"Function definition requires colon at end: def greet(name): — SyntaxError without it."},
    {id:2,question:"Find the bug:\n\nx = 5\nif x = 5:\n    print('Five')",options:["Missing colon","= should be ==","Wrong indentation","print syntax"],answer:"= should be ==",explanation:"= is assignment, == is comparison. if x == 5: is correct for condition checking."},
    {id:3,question:"Find the bug:\n\narr = [1, 2, 3]\nprint(arr[3])",options:["Wrong bracket","IndexError — valid indices 0,1,2","arr not defined","print syntax"],answer:"IndexError — valid indices 0,1,2",explanation:"arr has 3 elements at indices 0,1,2. arr[3] is out of range — IndexError."},
    {id:4,question:"Find the bug:\n\ndef add(a, b):\n    return a + b\n\nresult = add(2)\nprint(result)",options:["Missing return type","TypeError — missing required argument b","Wrong function name","print error"],answer:"TypeError — missing required argument b",explanation:"add() requires 2 arguments but only 1 given. TypeError: add() missing 1 required positional argument."},
    {id:5,question:"Find the bug:\n\nnum = input('Enter number: ')\nresult = num + 5\nprint(result)",options:["Wrong variable name","TypeError — input() returns string, not int","Missing print","Wrong operator"],answer:"TypeError — input() returns string, not int",explanation:"input() always returns string. Use int(input()) to convert. 'str' + int raises TypeError."},
    {id:6,question:"Find the bug:\n\nmy_list = [1, 2, 3]\nmy_list[0] = 10\nmy_tuple = (1, 2, 3)\nmy_tuple[0] = 10",options:["List cannot be modified","TypeError — tuples are immutable","Wrong index","Syntax error"],answer:"TypeError — tuples are immutable",explanation:"Tuples are immutable — elements cannot be changed after creation. TypeError: 'tuple' object does not support item assignment."},
    {id:7,question:"Find the bug:\n\nfor i in range(5):\nprint(i)",options:["range(5) is wrong","IndentationError — print must be indented","Missing colon","Wrong variable"],answer:"IndentationError — print must be indented",explanation:"Python uses indentation for blocks. print(i) must be indented inside the for loop."},
    {id:8,question:"Find the bug:\n\nresult = 10 / 0\nprint(result)",options:["Wrong operator","ZeroDivisionError","Syntax error","Type error"],answer:"ZeroDivisionError",explanation:"Dividing by zero raises ZeroDivisionError in Python. Handle with try/except."},
    {id:9,question:"Find the bug:\n\nname = 'Ali'\nprint('Hello' + name + 'How are you?')",options:["No bug","Missing spaces around name — output looks wrong","Wrong concatenation","print syntax"],answer:"Missing spaces around name — output looks wrong",explanation:"'Hello' + name = 'HelloAli'. Add spaces: 'Hello ' + name + ' How are you?' for proper output."},
    {id:10,question:"Find the bug:\n\nd = {'name': 'Ali'}\nprint(d['age'])",options:["Wrong dict syntax","KeyError — 'age' key does not exist","print error","Wrong bracket"],answer:"KeyError — 'age' key does not exist",explanation:"dict['key'] raises KeyError if key not found. Use d.get('age') or check 'age' in d first."},
  ]},
  { id:"py-bug-functions", name:"Bug: Functions", icon:"🐛", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"Find the bug:\n\ndef factorial(n):\n    if n == 0:\n        return 1\n    return n * factorial(n)\n\nprint(factorial(5))",options:["Missing base case","Infinite recursion — should be factorial(n-1)","Wrong operator","Return error"],answer:"Infinite recursion — should be factorial(n-1)",explanation:"factorial(n) calls itself with same n — infinite recursion. Should be factorial(n-1) to reduce."},
    {id:2,question:"Find the bug:\n\ndef append_to(element, to=[]):\n    to.append(element)\n    return to\n\nprint(append_to(1))\nprint(append_to(2))",options:["Wrong parameter","Mutable default argument — list shared across calls","Return error","append syntax"],answer:"Mutable default argument — list shared across calls",explanation:"Default mutable arguments are created once. to=[] persists between calls. Use to=None and create inside."},
    {id:3,question:"Find the bug:\n\ndef swap(a, b):\n    temp = a\n    a = b\n    b = temp\n\nx, y = 1, 2\nswap(x, y)\nprint(x, y)",options:["Swap logic wrong","Values not returned — Python ints immutable, local changes","Wrong temp","Syntax error"],answer:"Values not returned — Python ints immutable, local changes",explanation:"Python passes ints by value. Swapping inside function doesn't affect x,y outside. Return or use x,y=y,x."},
    {id:4,question:"Find the bug:\n\nglobal_var = 10\n\ndef modify():\n    global_var = 20\n\nmodify()\nprint(global_var)",options:["No bug","Missing global keyword — creates local variable instead","Wrong assignment","Function error"],answer:"Missing global keyword — creates local variable instead",explanation:"Without global global_var, assignment creates a new local variable. Add global global_var inside function."},
    {id:5,question:"Find the bug:\n\ndef divide(a, b):\n    return a / b\n\nresult = divide(10, 0)\nprint(result)",options:["Wrong operator","ZeroDivisionError — no error handling","Missing return","Wrong params"],answer:"ZeroDivisionError — no error handling",explanation:"divide(10, 0) raises ZeroDivisionError. Add try/except or check if b == 0 before dividing."},
    {id:6,question:"Find the bug:\n\ndef get_square(numbers):\n    squares = []\n    for n in numbers:\n        squares.append = n ** 2\n    return squares\n\nprint(get_square([1,2,3]))",options:["Wrong loop","= should be () — squares.append(n**2)","Wrong operator","Return error"],answer:"= should be () — squares.append(n**2)",explanation:"append is a method, not an attribute. squares.append = overwrites the method. Use squares.append(n**2)."},
    {id:7,question:"Find the bug:\n\ndef is_even(n):\n    if n % 2 = 0:\n        return True\n    return False",options:["Wrong logic","= should be == in condition","Missing else","Return type"],answer:"= should be == in condition",explanation:"In if condition, = is assignment (SyntaxError here). Use == for comparison: if n % 2 == 0."},
    {id:8,question:"Find the bug:\n\ndef mystery(lst):\n    lst = lst + [99]\n    return lst\n\narr = [1, 2, 3]\nmystery(arr)\nprint(arr)",options:["No bug — arr changes","arr not modified — + creates new list, original unchanged","Return error","Wrong operator"],answer:"arr not modified — + creates new list, original unchanged",explanation:"lst + [99] creates new list assigned to local lst. Original arr unchanged. Use lst.append(99) to modify in-place."},
    {id:9,question:"Find the bug:\n\ndef count_vowels(s):\n    count = 0\n    for char in s:\n        if char in 'aeiou':\n            count += 1\n    return count\n\nprint(count_vowels('Hello World'))",options:["No bug — returns 3","Bug — uppercase vowels missed — returns 3 not 4","Wrong loop","Return error"],answer:"Bug — uppercase vowels missed — returns 3 not 4",explanation:"'E', 'O' in 'Hello World' are uppercase, not in 'aeiou'. Use s.lower() first or check 'aeiouAEIOU'."},
    {id:10,question:"Find the bug:\n\nresult = []\nfor i in range(3):\n    result.append(lambda: i)\n\nprint([f() for f in result])",options:["Correct — [0,1,2]","Late binding — all lambdas use final i=2 — prints [2,2,2]","append error","Range error"],answer:"Late binding — all lambdas use final i=2 — prints [2,2,2]",explanation:"Lambdas capture variable i by reference, not value. When called, i=2 (final value). Fix: lambda i=i: i."},
  ]},
  { id:"py-bug-ds", name:"Bug: Data Structures", icon:"🐛", difficulty:"Medium", category:"DSA", questions:[
    {id:1,question:"Find the bug:\n\narr = [3, 1, 4, 1, 5]\nfor i in range(len(arr)):\n    for j in range(len(arr)):\n        if arr[j] > arr[j+1]:\n            arr[j], arr[j+1] = arr[j+1], arr[j]",options:["Wrong swap","IndexError — inner range should be len(arr)-1-i or len(arr)-1","Wrong condition","Outer loop"],answer:"IndexError — inner range should be len(arr)-1-i or len(arr)-1",explanation:"arr[j+1] when j=len(arr)-1 causes IndexError. Inner range should be range(len(arr)-1-i)."},
    {id:2,question:"Find the bug:\n\ndef binary_search(arr, target):\n    lo, hi = 0, len(arr)\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if arr[mid] == target: return mid\n        elif arr[mid] < target: lo = mid + 1\n        else: hi = mid - 1\n    return -1",options:["Wrong mid calc","hi should be len(arr)-1 — causes IndexError","Wrong return","Loop condition"],answer:"hi should be len(arr)-1 — causes IndexError",explanation:"len(arr) is out of bounds. hi should be len(arr)-1. Also hi = mid - 1 can miss elements — use hi = mid."},
    {id:3,question:"Find the bug:\n\nstack = []\nstack.append(1)\nstack.append(2)\nstack.append(3)\nprint(stack.pop(0))",options:["No bug","pop(0) removes from front — not stack behavior, use pop()","Stack full","Append error"],answer:"pop(0) removes from front — not stack behavior, use pop()",explanation:"Stack is LIFO. pop(0) removes first element (queue behavior). Stack pop should use pop() — removes last."},
    {id:4,question:"Find the bug:\n\nd = {}\nd['a'] = 1\nd['b'] = 2\nfor key in d:\n    d[key + '_new'] = d[key]\nprint(d)",options:["No bug","RuntimeError — dict size changed during iteration","Wrong loop","Key error"],answer:"RuntimeError — dict size changed during iteration",explanation:"Cannot modify dict size while iterating. Iterate over list(d.keys()) or d.copy() instead."},
    {id:5,question:"Find the bug:\n\nfrom collections import defaultdict\nfreq = defaultdict(int)\nwords = ['a', 'b', 'a', 'c', 'b', 'a']\nfor word in words:\n    freq[word] =+ 1\nprint(freq['a'])",options:["No bug","=+ should be += — freq['a'] stays 1","Wrong import","Wrong loop"],answer:"=+ should be += — freq['a'] stays 1",explanation:"=+ 1 means assign positive 1 each time (= (+1)). Should be += 1 to increment. freq['a'] will be 1 not 3."},
    {id:6,question:"Find the bug:\n\nclass Stack:\n    def __init__(self):\n        self.items = []\n    def push(self, item):\n        self.items.append(item)\n    def pop(self):\n        return self.items.pop(0)\n\ns = Stack()\ns.push(1); s.push(2); s.push(3)\nprint(s.pop())",options:["No bug","pop(0) makes it queue not stack — should be pop()","push error","items not initialized"],answer:"pop(0) makes it queue not stack — should be pop()",explanation:"Stack LIFO requires pop() from end. pop(0) removes from front making it FIFO (queue behavior)."},
    {id:7,question:"Find the bug:\n\ngraph = {0:[1,2], 1:[2], 2:[0]}\nvisited = set()\ndef dfs(node):\n    visited.add(node)\n    print(node)\n    for neighbor in graph[node]:\n        dfs(neighbor)\n\ndfs(0)",options:["No graph","Infinite loop — no visited check before recursive call","Wrong set","graph error"],answer:"Infinite loop — no visited check before recursive call",explanation:"No check if neighbor already visited causes infinite recursion. Add: if neighbor not in visited: dfs(neighbor)."},
    {id:8,question:"Find the bug:\n\npairs = [(1,'b'), (2,'a'), (3,'c')]\npairs.sort(key=lambda x: x[1])\nprint(pairs[0])",options:["No bug — (2,'a')","sort modifies nothing — need sorted()","Wrong lambda","Wrong index"],answer:"No bug — (2,'a')",explanation:"No bug! sort(key=lambda x: x[1]) sorts by second element. 'a' < 'b' < 'c' so (2,'a') is first."},
    {id:9,question:"Find the bug:\n\nmatrix = [[0]*3]*3\nmatrix[0][0] = 1\nprint(matrix)",options:["Correct — [[1,0,0],[0,0,0],[0,0,0]]","All rows modified — [[1,0,0],[1,0,0],[1,0,0]]","IndexError","Wrong init"],answer:"All rows modified — [[1,0,0],[1,0,0],[1,0,0]]",explanation:"[[0]*3]*3 creates 3 references to same list. Modifying one row modifies all. Use [[0]*3 for _ in range(3)]."},
    {id:10,question:"Find the bug:\n\ndef flatten(lst):\n    result = []\n    for item in lst:\n        if type(item) == list:\n            result.extend(flatten(item))\n        else:\n            result.append(item)\n    return result\n\nprint(flatten([1, [2, [3]], 4]))",options:["No bug — [1,2,3,4]","No bug but type(item)==list should be isinstance","Wrong base","extend error"],answer:"No bug but type(item)==list should be isinstance",explanation:"Works correctly for basic cases but type()==list misses subclasses. isinstance(item, list) is more Pythonic and robust."},
  ]},
  { id:"py-bug-oop", name:"Bug: OOP", icon:"🐛", difficulty:"Hard", category:"OOPs", questions:[
    {id:1,question:"Find the bug:\n\nclass Dog:\n    def __init__(name, age):\n        self.name = name\n        self.age = age\n\nd = Dog('Rex', 3)",options:["Wrong init name","Missing self parameter — first param must be self","Wrong assignment","Class syntax"],answer:"Missing self parameter — first param must be self",explanation:"Instance methods must have self as first parameter: def __init__(self, name, age). Without it, name gets the instance."},
    {id:2,question:"Find the bug:\n\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        self.breed = breed\n\nd = Dog('Rex', 'Lab')\nprint(d.name)",options:["No bug","AttributeError — super().__init__() not called","Wrong inheritance","Breed error"],answer:"AttributeError — super().__init__() not called",explanation:"Dog.__init__ doesn't call super().__init__(name), so self.name never set. d.name raises AttributeError."},
    {id:3,question:"Find the bug:\n\nclass Counter:\n    count = 0\n    def __init__(self):\n        self.count += 1\n\nc1 = Counter()\nc2 = Counter()\nprint(Counter.count)",options:["count is 2","count stays 0 — self.count creates instance attribute","Wrong init","Class variable error"],answer:"count stays 0 — self.count creates instance attribute",explanation:"self.count += 1 creates instance attribute, doesn't modify Counter.count. Use Counter.count += 1 instead."},
    {id:4,question:"Find the bug:\n\nclass MyList:\n    def __init__(self):\n        self.items = []\n    def add(self, item):\n        self.items.append(item)\n        return self\n\nml = MyList()\nml.add(1).add(2).add(3)\nprint(ml.items)",options:["Method chaining error","No bug — [1,2,3]","Return error","append error"],answer:"No bug — [1,2,3]",explanation:"Returning self enables method chaining. Each add() appends and returns MyList instance. Result: [1,2,3]."},
    {id:5,question:"Find the bug:\n\nclass Singleton:\n    instance = None\n    def __new__(cls):\n        if cls.instance is None:\n            cls.instance = super().__new__(cls)\n        return cls.instance\n\na = Singleton()\nb = Singleton()\nprint(a is b)",options:["False — two instances","True — no bug, singleton works","__new__ error","Class error"],answer:"True — no bug, singleton works",explanation:"__new__ checks if instance exists. Both a and b get same instance. a is b is True — correct Singleton."},
    {id:6,question:"Find the bug:\n\nclass Vehicle:\n    def start(self):\n        print('Vehicle starting')\n\nclass Car(Vehicle):\n    def start(self):\n        super.start()\n        print('Car starting')",options:["No bug","super.start() should be super().start() — missing ()","Wrong method","Print error"],answer:"super.start() should be super().start() — missing ()",explanation:"super is a class, not directly callable as attribute. super() with parentheses creates proxy object. TypeError without ()."},
    {id:7,question:"Find the bug:\n\nclass Temperature:\n    def __init__(self, celsius):\n        self.celsius = celsius\n    \n    @property\n    def fahrenheit(self):\n        return self.celsius * 9/5 + 32\n\nt = Temperature(100)\nt.fahrenheit = 212",options:["No bug","AttributeError — no setter defined for property","Wrong formula","Assignment error"],answer:"AttributeError — no setter defined for property",explanation:"@property creates read-only property by default. Setting t.fahrenheit raises AttributeError unless @fahrenheit.setter defined."},
    {id:8,question:"Find the bug:\n\nclass Stack:\n    def __init__(self):\n        self._items = []\n    def __len__(self):\n        return len(self._items)\n    def is_empty(self):\n        return self.__len__ == 0\n\ns = Stack()\nprint(s.is_empty())",options:["No bug","__len__ should be __len__() — comparing method to 0","Empty check","Wrong return"],answer:"__len__ should be __len__() — comparing method to 0",explanation:"self.__len__ is the method object, not its return value. Use self.__len__() or len(self) == 0."},
    {id:9,question:"Find the bug:\n\nclass Node:\n    def __init__(self, val):\n        self.val = val\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    def append(self, val):\n        new_node = Node(val)\n        if not self.head:\n            self.head = new_node\n        current = self.head\n        while current.next:\n            current = current.next\n        current.next = new_node",options:["No bug","Duplicate head — should return after setting head","Node error","Loop error"],answer:"Duplicate head — should return after setting head",explanation:"After self.head = new_node, code continues to while loop and sets current.next = new_node again. Add return after head assignment."},
    {id:10,question:"Find the bug:\n\nclass MathUtils:\n    @staticmethod\n    def add(a, b):\n        return a + b\n\n    def multiply(self, a, b):\n        return a * b\n\nprint(MathUtils.multiply(3, 4))",options:["No bug","TypeError — multiply needs instance (self)","Static error","Wrong params"],answer:"TypeError — multiply needs instance (self)",explanation:"multiply is instance method — needs self. MathUtils.multiply(3,4) passes 3 as self, 4 as a, b missing. Make it @staticmethod."},
  ]},
];

// ══════════════════════════════════════════════════
// PYTHON — FILL IN THE BLANK (⬜)
// ══════════════════════════════════════════════════
export const PYTHON_FILL_TOPICS: Topic[] = [
  { id:"py-fill-basics", name:"Fill: Basics", icon:"⬜", difficulty:"Easy", category:"Core", questions:[
    {id:1,question:"Complete to swap two variables:\n\na, b = 5, 10\na, b = _____, a\nprint(a, b)",options:["a","b","0","None"],answer:"b",explanation:"a, b = b, a is Python's idiomatic swap. No temp variable needed — tuple unpacking."},
    {id:2,question:"Complete the range to get [0,1,2,3,4]:\n\nfor i in _____(5):\n    print(i)",options:["range","list","iter","loop"],answer:"range",explanation:"range(5) generates 0,1,2,3,4. range(start, stop, step) is the full signature."},
    {id:3,question:"Complete the list comprehension:\n\nsquares = [x**2 _____ x in range(5)]\nprint(squares)",options:["from","for","in","while"],answer:"for",explanation:"List comprehension syntax: [expression for item in iterable]. [x**2 for x in range(5)] = [0,1,4,9,16]."},
    {id:4,question:"Complete to check if key exists in dict:\n\nd = {'name': 'Ali'}\nif 'name' _____ d:\n    print('Found')",options:["in","==","is","has"],answer:"in",explanation:"in operator checks if key exists in dictionary. 'name' in d returns True."},
    {id:5,question:"Complete to open file safely:\n\n_____ open('file.txt', 'r') as f:\n    content = f.read()",options:["open","try","with","using"],answer:"with",explanation:"with statement ensures file is properly closed after use — context manager. No need for explicit f.close()."},
    {id:6,question:"Complete the ternary expression:\n\nx = 10\nresult = 'even' _____ x % 2 == 0 _____ 'odd'",options:["if else","? :","when else","if or"],answer:"if else",explanation:"Python ternary: value_if_true if condition else value_if_false. result = 'even' if x%2==0 else 'odd'."},
    {id:7,question:"Complete to unpack list:\n\nnumbers = [1, 2, 3, 4, 5]\nfirst, *_____, last = numbers\nprint(rest)",options:["rest","middle","args","items"],answer:"rest",explanation:"*rest captures remaining elements. first=1, rest=[2,3,4], last=5. * in unpacking = starred expression."},
    {id:8,question:"Complete the enumerate loop:\n\nfruits = ['apple', 'banana', 'cherry']\nfor i, fruit in _____(fruits):\n    print(i, fruit)",options:["range","zip","enumerate","index"],answer:"enumerate",explanation:"enumerate() returns (index, value) pairs. for i, fruit in enumerate(fruits) gives index and value together."},
    {id:9,question:"Complete to merge two dicts (Python 3.9+):\n\nd1 = {'a': 1}\nd2 = {'b': 2}\nmerged = d1 _____ d2",options:["+","|","&","merge"],answer:"|",explanation:"| operator merges dicts in Python 3.9+. d1 | d2 = {'a':1, 'b':2}. Earlier: {**d1, **d2}."},
    {id:10,question:"Complete to get dict value safely:\n\nd = {'name': 'Ali'}\nage = d._____(('age', 0))\nprint(age)",options:["get","fetch","find","pop"],answer:"get",explanation:"d.get('age', 0) returns 0 if 'age' not found — safe alternative to d['age'] which raises KeyError."},
  ]},
  { id:"py-fill-functions", name:"Fill: Functions", icon:"⬜", difficulty:"Medium", category:"Core", questions:[
    {id:1,question:"Complete the decorator:\n\nimport functools\n\ndef my_decorator(func):\n    @functools._____\n    def wrapper(*args, **kwargs):\n        return func(*args, **kwargs)\n    return wrapper",options:["decorator","wraps(func)","wrap","preserve"],answer:"wraps(func)",explanation:"@functools.wraps(func) preserves original function's metadata (__name__, __doc__) in the wrapper."},
    {id:2,question:"Complete to create generator:\n\ndef count_up(n):\n    for i in range(n):\n        _____ i\n\nfor x in count_up(3):\n    print(x)",options:["return","yield","send","emit"],answer:"yield",explanation:"yield makes function a generator — produces values lazily. Pauses and resumes execution."},
    {id:3,question:"Complete the recursive function:\n\ndef factorial(n):\n    if n _____ 1:\n        return 1\n    return n * factorial(n - 1)",options:["==","<=","is",">="],answer:"<=",explanation:"Base case: n <= 1 handles both n=0 and n=1 returning 1. factorial(0)=1, factorial(1)=1 are both correct."},
    {id:4,question:"Complete to cache function results:\n\nfrom functools import _____\n\n@lru_cache(maxsize=None)\ndef fib(n):\n    if n < 2: return n\n    return fib(n-1) + fib(n-2)",options:["cache","lru_cache","memoize","cached"],answer:"lru_cache",explanation:"functools.lru_cache memoizes function results. maxsize=None = unlimited cache. @cache is simpler Python 3.9+."},
    {id:5,question:"Complete the closure:\n\ndef make_adder(n):\n    def adder(x):\n        return x + _____\n    return adder\n\nadd5 = make_adder(5)\nprint(add5(3))",options:["x","n","adder","self"],answer:"n",explanation:"Closure captures n from outer function. adder uses n from enclosing make_adder scope. add5(3) = 3+5 = 8."},
    {id:6,question:"Complete to pass keyword args:\n\ndef info(**kwargs):\n    for key, value in kwargs._____():\n        print(f'{key}: {value}')\n\ninfo(name='Ali', age=25)",options:["items","keys","values","pairs"],answer:"items",explanation:"dict.items() returns (key, value) pairs. kwargs.items() iterates over keyword arguments."},
    {id:7,question:"Complete the type hint:\n\nfrom typing import List\n\ndef get_evens(numbers: List[int]) -> _____[int]:\n    return [n for n in numbers if n % 2 == 0]",options:["Set","Tuple","List","Dict"],answer:"List",explanation:"Return type hint List[int] indicates function returns a list of integers. from typing import List needed pre-3.9."},
    {id:8,question:"Complete the partial function:\n\nfrom functools import _____\n\ndef power(base, exp):\n    return base ** exp\n\nsquare = partial(power, exp=2)\nprint(square(5))",options:["reduce","partial","curry","wrap"],answer:"partial",explanation:"functools.partial creates new function with some args pre-filled. square = partial(power, exp=2), square(5) = 25."},
    {id:9,question:"Complete the context manager:\n\nclass Timer:\n    def __enter__(self):\n        import time\n        self.start = time.time()\n        return self\n    def _____(self, *args):\n        import time\n        self.elapsed = time.time() - self.start",options:["__exit__","__close__","__end__","__finally__"],answer:"__exit__",explanation:"Context manager protocol: __enter__ on entry, __exit__ on exit (even if exception). with Timer() as t: pattern."},
    {id:10,question:"Complete the async function:\n\nimport asyncio\n\n_____ def fetch_data():\n    await asyncio.sleep(1)\n    return 'data'\n\nasyncio.run(fetch_data())",options:["async","await","def","coroutine"],answer:"async",explanation:"async def creates coroutine function. await pauses execution. asyncio.run() runs the coroutine."},
  ]},
  { id:"py-fill-ds", name:"Fill: Data Structures", icon:"⬜", difficulty:"Medium", category:"DSA", questions:[
    {id:1,question:"Complete the binary search:\n\ndef binary_search(arr, target):\n    lo, hi = 0, len(arr) - 1\n    while lo <= hi:\n        mid = (lo + hi) _____ 2\n        if arr[mid] == target: return mid\n        elif arr[mid] < target: lo = mid + 1\n        else: hi = mid - 1\n    return -1",options:["/","//","*","%"],answer:"//",explanation:"// is integer floor division — ensures mid is always an integer index. / would give float in Python 3."},
    {id:2,question:"Complete to reverse a linked list:\n\ndef reverse(head):\n    prev = None\n    current = head\n    while current:\n        next_node = current.next\n        current.next = _____\n        prev = current\n        current = next_node\n    return prev",options:["current","head","None","prev"],answer:"prev",explanation:"current.next = prev reverses the pointer. prev starts as None (new tail), progressively moves forward."},
    {id:3,question:"Complete the stack using list:\n\nstack = []\n# Push\nstack._____(10)\n# Pop\ntop = stack.pop()\nprint(top)",options:["insert","append","push","add"],answer:"append",explanation:"Python list append() adds to end (top of stack). pop() removes from end. append + pop = LIFO stack."},
    {id:4,question:"Complete BFS using deque:\n\nfrom collections import _____\n\ndef bfs(graph, start):\n    visited = set()\n    queue = deque([start])\n    while queue:\n        node = queue.popleft()\n        if node not in visited:\n            visited.add(node)\n            queue.extend(graph[node])",options:["queue","deque","Queue","OrderedDict"],answer:"deque",explanation:"collections.deque has O(1) popleft(). list.pop(0) is O(n). deque is efficient double-ended queue."},
    {id:5,question:"Complete the heap push:\n\nimport heapq\n\nmin_heap = []\nheapq._____(min_heap, 5)\nheapq._____(min_heap, 3)\nheapq._____(min_heap, 7)\nprint(min_heap[0])",options:["push / push / push","heappush / heappush / heappush","add / add / add","insert / insert / insert"],answer:"heappush / heappush / heappush",explanation:"heapq.heappush(heap, item) adds item maintaining heap property. min_heap[0] is always minimum = 3."},
    {id:6,question:"Complete the graph DFS:\n\ndef dfs(graph, node, visited=None):\n    if visited is _____:\n        visited = set()\n    visited.add(node)\n    for neighbor in graph[node]:\n        if neighbor not in visited:\n            dfs(graph, neighbor, _____)",options:["None / visited","[] / []","set() / set()","False / visited"],answer:"None / visited",explanation:"visited=None default (avoid mutable default). Pass visited set to recursive calls to track visited nodes."},
    {id:7,question:"Complete to find two sum:\n\ndef two_sum(nums, target):\n    seen = {}\n    for i, num in enumerate(nums):\n        complement = target - num\n        if complement in seen:\n            return [seen[_____], i]\n        seen[num] = i\n    return []",options:["num","complement","i","target"],answer:"complement",explanation:"seen stores num:index. complement's index is seen[complement]. Return [seen[complement], i] — both indices."},
    {id:8,question:"Complete the LRU Cache structure:\n\nfrom collections import _____\n\nclass LRUCache:\n    def __init__(self, capacity):\n        self.capacity = capacity\n        self.cache = OrderedDict()\n\n    def get(self, key):\n        if key not in self.cache: return -1\n        self.cache.move_to_end(key)\n        return self.cache[key]",options:["defaultdict","OrderedDict","Counter","deque"],answer:"OrderedDict",explanation:"OrderedDict tracks insertion order and has move_to_end(). Perfect for LRU — move accessed item to end, evict from front."},
    {id:9,question:"Complete the trie insert:\n\nclass TrieNode:\n    def __init__(self):\n        self.children = {}\n        self.is_end = False\n\nclass Trie:\n    def __init__(self):\n        self.root = TrieNode()\n    def insert(self, word):\n        node = self.root\n        for char in word:\n            if char not in node.children:\n                node.children[char] = TrieNode()\n            node = node.children[_____]\n        node.is_end = True",options:["word","node","char","self"],answer:"char",explanation:"node.children[char] moves to next node for current character. After loop, mark end of word."},
    {id:10,question:"Complete the dynamic programming solution:\n\ndef longest_common_subsequence(s1, s2):\n    m, n = len(s1), len(s2)\n    dp = [[0]*(n+1) for _ in range(m+1)]\n    for i in range(1, m+1):\n        for j in range(1, n+1):\n            if s1[i-1] == s2[j-1]:\n                dp[i][j] = dp[i-1][j-1] + _____\n            else:\n                dp[i][j] = max(dp[i-1][j], dp[i][j-1])\n    return dp[m][n]",options:["0","1","2","dp[i][j]"],answer:"1",explanation:"When characters match, LCS length = diagonal + 1. dp[i][j] = dp[i-1][j-1] + 1."},
  ]},
  { id:"py-fill-oop", name:"Fill: OOP", icon:"⬜", difficulty:"Hard", category:"OOPs", questions:[
    {id:1,question:"Complete the abstract class:\n\nfrom abc import ABC, _____\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self):\n        pass",options:["abstract","abstractmethod","AbstractMethod","Interface"],answer:"abstractmethod",explanation:"abstractmethod decorator marks method as abstract. Classes inheriting Shape must implement area()."},
    {id:2,question:"Complete the property setter:\n\nclass Temperature:\n    def __init__(self):\n        self._celsius = 0\n    @property\n    def celsius(self):\n        return self._celsius\n    @celsius._____\n    def celsius(self, value):\n        self._celsius = value",options:["set","setter","value","assign"],answer:"setter",explanation:"@property.setter decorator defines setter. @celsius.setter enables t.celsius = 25 assignment."},
    {id:3,question:"Complete the class method:\n\nclass Person:\n    count = 0\n    def __init__(self, name):\n        self.name = name\n        Person.count += 1\n    @_____\n    def get_count(cls):\n        return cls.count",options:["staticmethod","classmethod","property","method"],answer:"classmethod",explanation:"@classmethod receives cls (class) as first argument. Can access class variables. Called as Person.get_count()."},
    {id:4,question:"Complete the __repr__:\n\nclass Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    def _____:\n        return f'Point({self.x}, {self.y})'",options:["__str__(self)","__repr__(self)","__print__(self)","__format__(self)"],answer:"__repr__(self)",explanation:"__repr__ is for developers — unambiguous representation. __str__ is user-friendly. repr(obj) calls __repr__."},
    {id:5,question:"Complete the __eq__ method:\n\nclass Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    def __eq__(self, other):\n        return self.x == other.x _____ self.y == other.y",options:["or","and","&","|"],answer:"and",explanation:"Points are equal only if BOTH x and y match. and requires all conditions True."},
    {id:6,question:"Complete the mixin:\n\nclass JSONMixin:\n    def to_json(self):\n        import json\n        return json.dumps(self._____)\n\nclass User(JSONMixin):\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\nu = User('Ali', 25)\nprint(u.to_json())",options:["items","__dict__","vars","data"],answer:"__dict__",explanation:"__dict__ returns object's attribute dictionary. json.dumps(self.__dict__) serializes all instance attributes."},
    {id:7,question:"Complete the __slots__:\n\nclass Point:\n    _____ = ['x', 'y']\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y",options:["__dict__","__slots__","__attrs__","__fields__"],answer:"__slots__",explanation:"__slots__ restricts attributes and reduces memory — no __dict__ created per instance. Only listed attributes allowed."},
    {id:8,question:"Complete the metaclass:\n\nclass SingletonMeta(type):\n    _instances = {}\n    def __call__(cls, *args, **kwargs):\n        if cls not in cls._instances:\n            cls._instances[cls] = _____(cls, *args, **kwargs)\n        return cls._instances[cls]",options:["type()","super().__call__","cls()","new()"],answer:"super().__call__",explanation:"super().__call__ creates the actual instance. Metaclass __call__ intercepts instantiation — checks if instance exists."},
    {id:9,question:"Complete the descriptor:\n\nclass Validator:\n    def __set_name__(self, owner, name):\n        self.name = name\n    def __get__(self, obj, type=None):\n        return obj.__dict__.get(self.name)\n    def _____(self, obj, value):\n        if not isinstance(value, int):\n            raise TypeError('Must be int')\n        obj.__dict__[self.name] = value",options:["__set__","__assign__","__put__","__store__"],answer:"__set__",explanation:"Descriptor protocol: __get__, __set__, __delete__. __set__ called on attribute assignment — enables validation."},
    {id:10,question:"Complete dataclass with default factory:\n\nfrom dataclasses import dataclass, _____\n\n@dataclass\nclass Student:\n    name: str\n    grades: list = field(default_factory=_____)",options:["field / list","Field / list","field / []","Field / []"],answer:"field / list",explanation:"dataclass field with default_factory=list creates new empty list per instance. Using [] directly would be shared mutable default."},
  ]},
];